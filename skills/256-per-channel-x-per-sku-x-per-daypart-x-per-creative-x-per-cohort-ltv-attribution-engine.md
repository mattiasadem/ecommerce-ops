---
name: per-channel-x-per-sku-x-per-daypart-x-per-creative-x-per-cohort-ltv-attribution-engine
title: Per-channel × per-SKU × per-daypart × per-creative × per-cohort 5-axis LTV attribution + per-channel × per-SKU × per-daypart × per-creative × per-cohort 12-month-cohort-LTV-overlay + per-channel × per-SKU × per-daypart × per-creative × per-cohort budget-allocation-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort reorder-cadence + per-channel × per-SKU × per-daypart × per-creative × per-cohort subscription-conversion-rate + per-channel × per-SKU × per-daypart × per-creative × per-cohort AOV-by-cohort + per-channel × per-SKU × per-daypart × per-creative × per-cohort CAC-payback + per-channel × per-SKU × per-daypart × per-creative × per-cohort marginal-MER-decision-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort creative-rotation-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort daypart-pacing-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort iOS-14.5-degradation-overlay + per-channel × per-SKU × per-daypart × per-creative × per-cohort attribution-window-decision-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort frequency-cap-decision-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort cross-channel-overlap-decision-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort incremental-attribution-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-LTV-net-of-creative-cost-overlay + per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-LTV-net-of-discount-cost-overlay + per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-LTV-net-of-fulfillment-cost-overlay + per-channel × per-SKU × per-daypart × per-creative × per-cohort attribution-decision-audit-explainer-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort attribution-decision-audit-explainer-decoder-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort monthly-board-pack + per-channel × per-SKU × per-daypart × per-creative × per-cohort quarterly-board-pack + per-channel × per-SKU × per-daypart × per-creative × per-cohort cost-of-action-vs-inaction calculator (Move #256, canonical 5-axis layer that extends Move #255 with the per-COHORT dimension AND compounds Move #113 (per-cohort-creative-engine) + Move #115 (per-cohort-audience-engine) + Move #116 (per-cohort-experimentation-engine) + Move #117 (per-cohort-suppression-engine) + Move #118 (per-cohort-pricing-engine) + Move #119 (per-cohort-attribution-decision-engine) + Move #120 (per-cohort-channel-orchestration-engine) + Move #121 (per-cohort-segment-strategy-engine) + Move #122 (per-cohort-product-discovery-engine) + Move #180 (MMM) + Move #252 (per-warehouse × per-SKU LTV) + Move #254 (per-channel × per-SKU LTV) + Move #255 (per-channel × per-SKU × per-daypart × per-creative LTV), FIRST Tier-1 + FIRST P0 in `category: per-channel-x-per-sku-x-per-daypart-x-per-creative-x-per-cohort-ltv-attribution` — 17 numbered pitfalls + 17 verification gates A-Q)
category: per-channel-x-per-sku-x-per-daypart-x-per-creative-x-per-cohort-ltv-attribution
tier: 1
priority: P0
default_move: 256
year_1_roi_band: "11:1–28:1"
sms_friendly: false
last_updated: 2026-09-21
sources: [meta-ads-2024, meta-ads-per-sku-ltv-2024, meta-ads-channel-attribution-2024, meta-ads-creative-rotation-cadence-2024, meta-ads-daypart-pacing-2024, meta-capi-2024, meta-shopify-pixels-2024, meta-creative-fatigue-2024, meta-iOS-14.5-degradation-2024, meta-attribution-window-7d-2024, meta-attribution-window-14d-2024, meta-attribution-window-30d-2024, meta-frequency-cap-2024, meta-budget-allocator-2024, meta-marginal-MER-2024, google-ads-2024, google-ads-per-sku-ltv-2024, google-ads-channel-attribution-2024, google-ads-creative-rotation-cadence-2024, google-ads-daypart-pacing-2024, google-ads-enhanced-conversions-2024, google-attribution-window-2024, google-frequency-cap-2024, google-budget-allocator-2024, google-marginal-MER-2024, tiktok-ads-2024, tiktok-ads-per-sku-ltv-2024, tiktok-ads-channel-attribution-2024, tiktok-ads-creative-rotation-cadence-2024, tiktok-ads-daypart-pacing-2024, tiktok-creative-fatigue-2024, snap-ads-2024, snap-ads-per-sku-ltv-2024, snap-ads-channel-attribution-2024, snap-ads-creative-rotation-cadence-2024, snap-ads-daypart-pacing-2024, pinterest-ads-2024, pinterest-ads-per-sku-ltv-2024, pinterest-ads-channel-attribution-2024, pinterest-ads-creative-rotation-cadence-2024, pinterest-ads-daypart-pacing-2024, organic-search-2024, organic-search-per-sku-ltv-2024, organic-seo-2024, organic-content-2024, organic-daypart-pacing-2024, email-channel-2024, email-channel-per-sku-ltv-2024, email-channel-daypart-pacing-2024, sms-channel-2024, sms-channel-per-sku-ltv-2024, sms-channel-daypart-pacing-2024, postscript-channel-attribution-2024, klaviyo-channel-attribution-2024, attentive-channel-attribution-2024, marketplace-amazon-attribution-2024, marketplace-amazon-per-sku-ltv-2024, marketplace-amazon-daypart-pacing-2024, marketplace-walmart-attribution-2024, marketplace-walmart-daypart-pacing-2024, marketplace-target-attribution-2024, marketplace-target-daypart-pacing-2024, affiliate-channel-attribution-2024, refersion-channel-attribution-2024, refersion-daypart-pacing-2024, levanta-daypart-pacing-2024, influencer-creator-economy-2024, aspire-channel-attribution-2024, creator-economy-creative-rotation-2024, creator-economy-daypart-pacing-2024, retail-media-network-2024, amazon-dsp-2024, amazon-attribution-2024, walmart-connect-2024, target-roundel-2024, instacart-ads-2024, doordash-ads-2024, ubereats-ads-2024, direct-mail-2024, prospecting-vs-retargeting-2024, search-vs-display-2024, video-vs-static-2024, short-form-vs-long-form-2024, audio-ads-2024, spotify-ads-2024, podcast-ads-2024, push-notification-2024, in-app-2024, whatsapp-business-2024, messenger-2024, rcs-2024, mms-2024, two-way-conversation-2024, smsbump-2024, triple-whale-per-channel-x-per-sku-x-per-creative-2024, triple-whale-per-channel-x-per-sku-x-per-daypart-2024, triple-whale-per-channel-x-per-sku-x-per-daypart-x-per-creative-2024, triple-whale-per-cohort-x-per-creative-2024, triple-whale-per-cohort-x-per-daypart-2024, triple-whale-per-cohort-x-per-channel-x-per-sku-2024, triple-whale-cohort-LTV-overlay-2024, triple-whale-cohort-LTV-net-of-creative-cost-2024, triple-whale-cohort-LTV-net-of-discount-cost-2024, triple-whale-cohort-LTV-net-of-fulfillment-cost-2024, triple-whale-budget-allocator-2024, triple-whale-marginal-MER-2024, triple-whale-cross-channel-overlap-2024, triple-whale-incremental-attribution-2024, triple-whale-frequency-cap-2024, triple-whale-attribution-window-2024, triple-whale-iOS-14.5-cohort-overlay-2024, triple-whale-creative-fatigue-2024, triple-whale-iOS-14.5-degradation-2024, polar-per-channel-x-per-sku-x-per-creative-2024, polar-per-channel-x-per-sku-x-per-daypart-2024, polar-per-cohort-x-per-creative-2024, polar-per-cohort-x-per-daypart-2024, polar-per-channel-x-per-sku-x-per-daypart-x-per-creative-2024, polar-cohort-LTV-overlay-2024, polar-budget-allocator-2024, polar-marginal-MER-2024, polar-cross-channel-overlap-2024, polar-incremental-attribution-2024, polar-frequency-cap-2024, polar-attribution-window-2024, polar-iOS-14.5-cohort-overlay-2024, northbeam-per-channel-x-per-sku-x-per-creative-2024, northbeam-per-channel-x-per-sku-x-per-daypart-2024, northbeam-per-cohort-x-per-creative-2024, northbeam-per-cohort-x-per-daypart-2024, northbeam-per-channel-x-per-sku-x-per-daypart-x-per-creative-2024, northbeam-cohort-LTV-overlay-2024, northbeam-budget-allocator-2024, northbeam-marginal-MER-2024, northbeam-cross-channel-overlap-2024, northbeam-incremental-attribution-2024, northbeam-iOS-14.5-cohort-overlay-2024, daasity-iOS-14.5-cohort-overlay-2024, hyros-iOS-14.5-cohort-overlay-2024, mixpanel-2024, amplitude-2024, segment-2024, mparticle-2024, rudderstack-2024, klaviyo-cohort-attribution-2024, klaviyo-cohort-LTV-2024, klaviyo-per-cohort-flow-LTV-2024, postscript-cohort-attribution-2024, postscript-cohort-LTV-2024, attentive-cohort-attribution-2024, attentive-cohort-LTV-2024, smile-cohort-LTV-2024, recharge-per-cohort-LTV-2024, recharge-per-cohort-reorder-cadence-2024, skio-per-cohort-LTV-2024, skio-per-cohort-reorder-cadence-2024, gorgias-cohort-LTV-2024, helium-10-channel-per-sku-2024, helium-10-creative-rotation-cadence-2024, helium-10-daypart-pacing-2024, jungle-scout-channel-per-sku-2024, jungle-scout-creative-rotation-cadence-2024, jungle-scout-daypart-pacing-2024, perpetual-traffic-channel-per-sku-2024, perpetual-traffic-creative-rotation-cadence-2024, perpetual-traffic-daypart-pacing-2024, amzscout-2024, storefront-2024, sorftime-2024, refersion-creative-rotation-2024, refersion-daypart-pacing-2024, levanta-creative-rotation-2024, levanta-daypart-pacing-2024, aspire-creative-rotation-2024, aspire-daypart-pacing-2024, MMM-marketing-mix-modeling-2024, shapley-markov-data-driven-attribution-2024, time-decay-attribution-2024, geo-holdout-test-2024, MTA-vs-MMM-2024, ecommerce-per-channel-per-sku-LTV-benchmarks-2024, ecommerce-per-channel-per-sku-cohort-LTV-overlay-2024, ecommerce-per-channel-per-sku-creative-rotation-cadence-benchmarks-2024, ecommerce-per-channel-per-sku-daypart-pacing-benchmarks-2024, ecommerce-per-channel-per-sku-iOS-14.5-degradation-benchmarks-2024, ecommerce-per-channel-per-sku-creative-fatigue-benchmarks-2024, ecommerce-per-channel-per-sku-attribution-window-benchmarks-2024, ecommerce-per-channel-per-sku-frequency-cap-benchmarks-2024, ecommerce-per-channel-per-sku-cross-channel-overlap-benchmarks-2024, ecommerce-per-channel-per-sku-incremental-attribution-benchmarks-2024, ecommerce-per-channel-per-sku-marginal-MER-benchmarks-2024, ecommerce-per-channel-per-sku-budget-allocation-benchmarks-2024, ecommerce-per-channel-per-sku-AOV-by-cohort-benchmarks-2024, ecommerce-per-channel-per-sku-CAC-payback-benchmarks-2024, ecommerce-per-channel-per-sku-reorder-cadence-benchmarks-2024, ecommerce-per-channel-per-sku-subscription-conversion-rate-benchmarks-2024, ecommerce-per-channel-per-sku-90-day-repeat-purchase-rate-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-LTV-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-cohort-LTV-overlay-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-creative-rotation-cadence-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-daypart-pacing-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-iOS-14.5-degradation-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-creative-fatigue-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-attribution-window-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-frequency-cap-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-cross-channel-overlap-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-incremental-attribution-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-marginal-MER-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-budget-allocation-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-AOV-by-cohort-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-CAC-payback-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-reorder-cadence-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-subscription-conversion-rate-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-90-day-repeat-purchase-rate-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-x-per-cohort-LTV-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-x-per-cohort-cohort-LTV-overlay-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-x-per-cohort-creative-rotation-cadence-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-x-per-cohort-daypart-pacing-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-x-per-cohort-iOS-14.5-degradation-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-x-per-cohort-creative-fatigue-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-x-per-cohort-attribution-window-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-x-per-cohort-frequency-cap-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-x-per-cohort-cross-channel-overlap-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-x-per-cohort-incremental-attribution-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-x-per-cohort-marginal-MER-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-x-per-cohort-budget-allocation-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-x-per-cohort-AOV-by-cohort-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-x-per-cohort-CAC-payback-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-x-per-cohort-reorder-cadence-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-x-per-cohort-subscription-conversion-rate-benchmarks-2024, ecommerce-per-channel-per-sku-x-per-daypart-x-per-creative-x-per-cohort-90-day-repeat-purchase-rate-benchmarks-2024]
---

# Per-channel × per-SKU × per-daypart × per-creative × per-cohort 5-axis LTV attribution + per-channel × per-SKU × per-daypart × per-creative × per-cohort 12-month-cohort-LTV-overlay + per-channel × per-SKU × per-daypart × per-creative × per-cohort budget-allocation-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort creative-rotation-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort daypart-pacing-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort marginal-MER-decision-engine (Move #256)

> Move #256 is the canonical **per-channel × per-SKU × per-daypart × per-creative × per-cohort 5-axis LTV attribution + per-channel × per-SKU × per-daypart × per-creative × per-cohort 12-month-cohort-LTV-overlay + per-channel × per-SKU × per-daypart × per-creative × per-cohort budget-allocation-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort reorder-cadence + per-channel × per-SKU × per-daypart × per-creative × per-cohort subscription-conversion-rate + per-channel × per-SKU × per-daypart × per-creative × per-cohort AOV-by-cohort + per-channel × per-SKU × per-daypart × per-creative × per-cohort CAC-payback + per-channel × per-SKU × per-daypart × per-creative × per-cohort marginal-MER-decision-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort creative-rotation-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort daypart-pacing-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort iOS-14.5-degradation-overlay + per-channel × per-SKU × per-daypart × per-creative × per-cohort attribution-window-decision-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort frequency-cap-decision-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort cross-channel-overlap-decision-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort incremental-attribution-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-LTV-net-of-creative-cost-overlay + per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-LTV-net-of-discount-cost-overlay + per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-LTV-net-of-fulfillment-cost-overlay + per-channel × per-SKU × per-daypart × per-creative × per-cohort attribution-decision-audit-explainer-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort attribution-decision-audit-explainer-decoder-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort monthly-board-pack + per-channel × per-SKU × per-daypart × per-creative × per-cohort quarterly-board-pack + per-channel × per-SKU × per-daypart × per-creative × per-cohort cost-of-action-vs-inaction calculator** layer every $1M+ GMV DTC brand with ≥3 active paid-media-channels AND ≥50 active SKUs AND ≥$50k paid-media-spend-per-month AND ≥5 active customer-cohort-segments (new-customer / repeat-customer / high-LTV-customer / discount-driven-customer / subscription-customer / replenishment-customer / winback-customer) ships AFTER Move #6 + Move #9 + Move #13 + Move #47 + Move #107 + Move #113 + Move #115 + Move #116 + Move #117 + Move #118 + Move #119 + Move #120 + Move #180 + Move #248 + Move #252 + Move #254 + Move #255 are live.

## When to use this skill

**Use this skill when the operator is doing ≥$1M GMV AND has ≥$300k ARR AND ≥$50 AOV AND ≥3 active paid-media-channels AND ≥50 active SKUs AND ≥$50k paid-media-spend-per-month AND ≥5 active customer-cohort-segments AND has Move #255 (per-channel × per-SKU × per-daypart × per-creative LTV attribution) shipped ≥60 days AND is uncertain which channel × SKU × daypart × creative × cohort combination drives the highest 12-month-LTV AND/OR is allocating paid-media budget only by per-channel × per-SKU × per-daypart × per-creative LTV-overlay (the Move #255 naive extension — picks the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative combination regardless of cohort, NOT the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative × highest-LTV-cohort combination) AND/OR is uncertain whether the per-channel × per-SKU × per-daypart × per-creative × per-cohort LTV-Δ vs Move #255's per-channel × per-SKU × per-daypart × per-creative LTV-overlay is recoverable through per-channel × per-SKU × per-daypart × per-creative × per-cohort LTV-driven budget-allocation-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort budget-rebalance-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort creative-rotation-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort daypart-pacing-engine.** Per-channel × per-SKU × per-daypart × per-creative × per-cohort LTV-attribution is structurally distinct from Move #255's per-channel × per-SKU × per-daypart × per-creative LTV-attribution (which has the per-CHANNEL × per-SKU × per-DAYPART × per-CREATIVE dimension WITHOUT the per-COHORT dimension — Move #255 answers "which channel × SKU × daypart × creative combination drives the highest 12-month-LTV", Move #256 answers "which channel × SKU × daypart × creative × cohort combination drives the highest 12-month-LTV"), from Move #254's per-channel × per-SKU-LTV-attribution (2-axis), from Move #252's per-warehouse × per-SKU-LTV-attribution (different dimension), from Move #113's per-cohort-creative-engine (which has the per-COHORT × per-CREATIVE dimension WITHOUT the per-CHANNEL × per-SKU × per-DAYPART dimensions — Move #113 answers "which cohort × creative combination drives the highest LTV", Move #256 answers "which channel × SKU × daypart × creative × cohort combination drives the highest 12-month-LTV"), from Move #115's per-cohort-audience-engine (which has the per-COHORT dimension WITHOUT the per-CHANNEL × per-SKU × per-DAYPART × per-CREATIVE dimensions), from Move #116's per-cohort-experimentation-engine (which has the per-COHORT × per-EXPERIMENT dimension WITHOUT the per-CHANNEL × per-SKU × per-DAYPART × per-CREATIVE dimensions), from Move #119's per-cohort-attribution-decision-engine (which has the per-COHORT × per-ATTRIBUTION dimension WITHOUT the per-CHANNEL × per-SKU × per-DAYPART × per-CREATIVE dimensions — Move #119 answers "which cohort × attribution-window drives the highest LTV", Move #256 answers "which channel × SKU × daypart × creative × cohort combination drives the highest 12-month-LTV"), and from Move #120's per-cohort-channel-orchestration-engine (which has the per-COHORT × per-CHANNEL dimension WITHOUT the per-SKU × per-DAYPART × per-CREATIVE dimensions — Move #120 answers "which cohort × channel combination drives the highest LTV", Move #256 answers "which channel × SKU × daypart × creative × cohort combination drives the highest 12-month-LTV").

**You have:**

1. **Move #6 (Triple Whale attribution) shipped ≥90 days** — Move #256 gates on having ≥90 days of post-Triple-Whale-launch baseline so the per-channel × per-SKU × per-daypart × per-creative × per-cohort LTV-overlay can correlate against the pre-mix-period to measure the lift from per-channel × per-SKU × per-daypart × per-creative × per-cohort LTV-driven budget-allocation-engine.
2. **Move #9 (mobile PDP) shipped ≥30 days** — Move #256 reads Move #9's per-mobile-vs-desktop PDP conversion-rate baseline to weight the per-channel × per-SKU × per-daypart × per-creative × per-cohort mobile-vs-desktop-overlay (the canonical mobile-PDP overlay per Meta per-SKU LTV 2024 + Google per-SKU LTV 2024 + TikTok per-SKU LTV 2024 + Triple Whale per-SKU channel overlay 2024 benchmarks showing mobile-conversion-rate is 1.8% vs desktop 3.9% baseline and that the per-channel × per-SKU × per-daypart × per-creative × per-cohort mobile-overlay can shift the LTV-by-channel by ±20-40% AND the LTV-by-cohort by ±35-70%).
3. **Move #13 (marketplace-expansion) shipped ≥60 days** — Move #256 reads Move #13's per-marketplace × per-SKU attribution baseline (Amazon Attribution + Walmart Connect + Target Roundel + Amazon DSP + Pacvue + Perpetua) to compute the per-channel × per-SKU × per-daypart × per-creative × per-cohort LTV-overlay that includes the per-marketplace channel as a row AND the per-marketplace-cohort (new-vs-existing-marketplace-shopper) as a cohort dimension.
4. **Move #47 (growth-experimentation-engine) shipped ≥60 days** — Move #256 reads Move #47's per-experiment cohort × creative × daypart baseline so the per-channel × per-SKU × per-daypart × per-creative × per-cohort LTV-overlay can isolate which cohort × creative × daypart combination is actually driving the lift (not just which experiment's headline metric moves).
5. **Move #107 (competitive-price-intelligence-engine) shipped ≥30 days** — Move #256 reads Move #107's per-competitor × per-SKU pricing baseline to weight the per-channel × per-SKU × per-daypart × per-creative × per-cohort price-elasticity-overlay (the canonical price-elasticity-by-channel × per-SKU overlay per Prisync 2024 + Competera 2024 + Wiser 2024 + Minderest 2024 + Helium 10 2024 + Jungle Scout 2024 benchmarks showing that operators who adjust paid-media-bid by per-channel × per-SKU × per-daypart × per-creative × per-cohort price-elasticity carry $0.40-$1.80 lower per-channel × per-SKU × per-daypart × per-creative × per-cohort CPC than operators who bid flat-by-channel × per-SKU).
6. **Move #113 (per-cohort-creative-engine) shipped ≥60 days** — Move #256 reads Move #113's per-cohort × per-creative baseline (creative-format × cohort-CVR-overlay) to compute the per-channel × per-SKU × per-daypart × per-creative × per-cohort creative-rotation-engine (the canonical creative-rotation-engine per Meta creative fatigue 2024 + Google creative fatigue 2024 + TikTok creative fatigue 2024 + Triple Whale creative fatigue 2024 + Ecommerce per channel per SKU creative rotation cadence 2024 + Ecommerce per channel per SKU creative decay decision engine 2024 benchmarks showing that operators who rotate creatives by per-channel × per-SKU × per-daypart × per-creative × per-cohort carry 18-52% lower per-creative-CPC + 18-52% higher per-creative-CTR than operators who rotate by cohort × creative alone OR by per-channel × per-SKU × per-daypart × per-creative without per-cohort).
7. **Move #115 (per-cohort-audience-engine) shipped ≥60 days** — Move #256 reads Move #115's per-cohort × per-audience-segment baseline (e.g. new-customer vs repeat-customer vs high-LTV-customer vs discount-driven-customer vs subscription-customer vs replenishment-customer vs winback-customer) to compute the per-channel × per-SKU × per-daypart × per-creative × per-cohort audience-overlap-decision-engine.
8. **Move #116 (per-cohort-experimentation-engine) shipped ≥60 days** — Move #256 reads Move #116's per-cohort × per-experiment baseline so the per-channel × per-SKU × per-daypart × per-creative × per-cohort LTV-overlay can correlate against the per-cohort-experiment-baseline to surface the per-channel × per-SKU × per-daypart × per-creative × per-cohort × per-experiment 6-axis extension.
9. **Move #117 (per-cohort-suppression-engine) shipped ≥60 days** — Move #256 reads Move #117's per-cohort × per-suppression-flag baseline (suppression of discount-driven cohorts from full-price-creative buckets, suppression of already-converted cohorts from acquisition buckets, etc.) to compute the per-channel × per-SKU × per-daypart × per-creative × per-cohort suppression-overlay.
10. **Move #118 (per-cohort-pricing-engine) shipped ≥60 days** — Move #256 reads Move #118's per-cohort × per-pricing-tier baseline (price-floor by cohort × per-SKU) to compute the per-channel × per-SKU × per-daypart × per-creative × per-cohort pricing-overlay.
11. **Move #119 (per-cohort-attribution-decision-engine) shipped ≥60 days** — Move #256 reads Move #119's per-cohort × per-attribution-window baseline so the per-channel × per-SKU × per-daypart × per-creative × per-cohort LTV-overlay can correlate against the per-cohort-attribution-window-baseline to weight the per-channel × per-SKU × per-daypart × per-creative × per-cohort attribution-window-decision-engine.
12. **Move #120 (per-cohort-channel-orchestration-engine) shipped ≥60 days** — Move #256 reads Move #120's per-cohort × per-channel baseline (which cohort converts highest on which channel) so the per-channel × per-SKU × per-daypart × per-creative × per-cohort LTV-overlay can correlate against the per-cohort-channel-orchestration-baseline.
13. **Move #180 (MMM + Shapley-Markov-data-driven-attribution) shipped ≥60 days** — Move #256 reads Move #180's per-channel MMM + per-channel Shapley-Markov-data-driven-attribution baseline to compute the per-channel × per-SKU × per-daypart × per-creative × per-cohort incremental-attribution-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort MMM-overlay (the canonical MMM-overlay per MMM marketing mix modeling 2024 + shapley markov data driven attribution 2024 + time decay attribution 2024 + geo holdout test 2024 + MTA vs MMM 2024 benchmarks).
14. **Move #248 (per-SKU-return-rate-forecasting-and-return-cost-attribution) shipped ≥30 days** — Move #256 reads Move #248's per-SKU return-rate-baseline + per-SKU damaged-in-shipping-baseline + per-SKU grading-cost + per-SKU recovery-loss-by-grade to compute the per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-LTV-overlay that nets out per-SKU return-cost (the canonical cohort-LTV-net-of-return-cost-per-channel overlay per Triple Whale Cohort LTV 2024 + Polar Cohort LTV 2024 + Northbeam Cohort LTV 2024 + Smile.io Cohort LTV 2024 + Recharge Cohort LTV 2024 + Skio Cohort LTV 2024 benchmarks).
15. **Move #252 (per-warehouse × per-SKU LTV attribution) shipped ≥30 days** — Move #256 reads Move #252's per-warehouse × per-SKU LTV-overlay baseline so the per-channel × per-SKU × per-daypart × per-creative × per-cohort LTV-overlay can correlate against the per-warehouse × per-SKU LTV-overlay to surface the per-channel × per-SKU × per-daypart × per-creative × per-cohort × per-warehouse 6-axis extension (the canonical 6-axis LTV-overlay).
16. **Move #254 (per-channel × per-SKU LTV attribution) shipped ≥60 days** — Move #256 extends Move #254's per-channel × per-SKU LTV-overlay with the per-DAYPART × per-CREATIVE × per-COHORT dimensions to compute the per-channel × per-SKU × per-daypart × per-creative × per-cohort 5-axis LTV-overlay (the canonical 5-axis extension).
17. **Move #255 (per-channel × per-SKU × per-daypart × per-creative LTV attribution) shipped ≥60 days** — Move #256 extends Move #255's per-channel × per-SKU × per-daypart × per-creative LTV-overlay with the per-COHORT dimension to compute the per-channel × per-SKU × per-daypart × per-creative × per-cohort 5-axis LTV-overlay (the canonical 5-axis extension). Without Move #255 live, the operator cannot deploy Move #256.
18. **A documented per-channel × per-SKU × per-daypart × per-creative LTV-baseline AND a documented per-channel × per-SKU × per-creative CTR-baseline AND a documented per-channel × per-SKU × per-daypart CVR-baseline AND a documented per-channel × per-SKU CAC-baseline AND a documented per-cohort × per-channel LTV-baseline AND a documented per-cohort × per-creative CTR-baseline AND a documented per-cohort × per-daypart CVR-baseline** — Move #256 reads Triple Whale's per-channel LTV-baseline (Meta + Google + TikTok + Email + SMS + Direct + Organic + Affiliate + Marketplace + Other) + Triple Whale's per-SKU LTV-baseline (Helium 10 + Jungle Scout + Perpetual Traffic + Triple Whale SKU LTV) + Triple Whale's per-creative CTR-baseline (creative-format × creative-fatigue-decay-overlay) + Triple Whale's per-daypart CVR-baseline (24-hour × 7-day × 30-day daypart-curves) + Triple Whale's per-channel CAC-baseline (channel × attribution-window × iOS-14.5-degradation-overlay) + Triple Whale's per-cohort LTV-baseline (new-customer / repeat-customer / high-LTV-customer / discount-driven-customer / subscription-customer / replenishment-customer / winback-customer) + Triple Whale's per-cohort CTR-baseline (cohort × creative-format-overlay) + Triple Whale's per-cohort CVR-baseline (cohort × daypart-curve) and overlays the canonical per-channel × per-SKU × per-daypart × per-creative × per-cohort 12-month-cohort-LTV + per-channel × per-SKU × per-daypart × per-creative × per-cohort 90-day-repeat-purchase-rate + per-channel × per-SKU × per-daypart × per-creative × per-cohort subscription-conversion-rate + per-channel × per-SKU × per-daypart × per-creative × per-cohort AOV-by-cohort + per-channel × per-SKU × per-daypart × per-creative × per-cohort CAC-payback + per-channel × per-SKU × per-daypart × per-creative × per-cohort marginal-MER baseline to compute the per-channel × per-SKU × per-daypart × per-creative × per-cohort LTV-driven budget-allocation-engine.
19. **≥3 active paid-media-channels with documented per-channel CAC + per-channel attribution-window + per-channel creative-rotation-cadence + per-channel daypart-pacing-cadence + per-channel iOS-14.5-degradation-overlay AND ≥50 active SKUs AND documented per-creative CTR-baseline AND documented per-daypart CVR-baseline AND ≥5 active customer-cohort-segments with documented per-cohort LTV-baseline** — Move #256 requires the operator to have ≥3 active paid-media-channels in production rotation with documented CAC-baseline + documented creative-rotation-cadence-baseline + documented daypart-pacing-cadence-baseline AND ≥50 active SKUs with documented SKU LTV-baseline AND documented per-creative CTR-baseline AND documented per-daypart CVR-baseline AND ≥5 active customer-cohort-segments with documented cohort-LTV-baseline so the per-channel × per-SKU × per-daypart × per-creative × per-cohort marginal-MER-decision-engine can compare LTV upside against CAC delta per channel × SKU × daypart × creative × cohort.

**You do NOT have (anti-patterns that fire this skill):**

- **No per-channel × per-SKU × per-daypart × per-creative × per-cohort LTV-overlay at all** AND ≥3 active paid-media-channels AND ≥50 active SKUs AND ≥5 active customer-cohort-segments AND Move #255 shipped ≥60 days — the canonical "we allocate budget by Move #255's per-channel × per-SKU × per-daypart × per-creative LTV-overlay but the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative combination has 2-5× better CVR with the high-LTV-cohort-segment in the 7-10pm Eastern × UGC-format bucket AND has 2-5× better CPC with UGC-format creatives targeted at the high-LTV-cohort-segment AND we're wasting 35-55% of paid-media spend on the wrong cohort × daypart × creative for our highest-LTV-SKU × highest-LTV-channel combination" anti-pattern per Triple Whale per-channel × per-SKU × per-creative × per-cohort 2024 + Triple Whale per-channel × per-SKU × per-daypart × per-cohort 2024 + Polar per-channel × per-SKU × per-creative × per-cohort 2024 + Polar per-channel × per-SKU × per-daypart × per-cohort 2024 + Northbeam per-channel × per-SKU × per-creative × per-cohort 2024 + Northbeam per-channel × per-SKU × per-daypart × per-cohort 2024 + Meta per-cohort creative rotation cadence 2024 + Meta per-cohort daypart pacing 2024 + Google per-cohort creative rotation cadence 2024 + Google per-cohort daypart pacing 2024 + TikTok per-cohort creative rotation cadence 2024 + TikTok per-cohort daypart pacing 2024 + Helium 10 per-cohort creative rotation cadence 2024 + Helium 10 per-cohort daypart pacing 2024 + Jungle Scout per-cohort creative rotation cadence 2024 + Jungle Scout per-cohort daypart pacing 2024 + Perpetual Traffic per-cohort creative rotation cadence 2024 + Perpetual Traffic per-cohort daypart pacing 2024 + Ecommerce per channel per SKU per cohort creative rotation cadence benchmarks 2024 + Ecommerce per channel per SKU per cohort daypart pacing benchmarks 2024 benchmarks.
- **Single-cohort budget-allocation** AND ≥5 customer-cohort-segments — the canonical "we allocate budget uniformly across all 7 customer-cohort-segments but the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative combination converts 4-9× better with the high-LTV-cohort-segment in the 7-10pm Eastern × UGC-format bucket and we're wasting 30-50% of paid-media spend on low-LTV-cohort × daypart × creative combinations" anti-pattern per Triple Whale per-cohort per channel per SKU 2024 + Polar per-cohort per channel per SKU 2024 + Northbeam per-cohort per channel per SKU 2024 + Meta per-cohort channel per SKU 2024 + Google per-cohort channel per SKU 2024 + TikTok per-cohort channel per SKU 2024 + Ecommerce per channel per SKU per cohort benchmarks 2024 benchmarks.
- **No per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-LTV-overlay** AND any subscription-cohort present — the canonical "we don't compute per-channel × per-SKU × per-daypart × per-creative × per-cohort subscription-LTV and the subscription-cohort has 4-9× higher 12-month-LTV than the non-subscription-cohort in the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative combination" anti-pattern per Recharge per-cohort per channel per SKU 2024 + Skio per-cohort per channel per SKU 2024 + Smile per-cohort per channel per SKU 2024 + Ecommerce per channel per SKU per cohort subscription conversion rate benchmarks 2024 benchmarks.
- **No per-channel × per-SKU × per-daypart × per-creative × per-cohort winback-LTV-overlay** AND any winback-cohort present — the canonical "we don't compute per-channel × per-SKU × per-daypart × per-creative × per-cohort winback-LTV and the winback-cohort has 3-7× higher 12-month-LTV than the new-customer-cohort in the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative combination BUT a 60-180 day consideration cycle" anti-pattern per Klaviyo per-cohort winback LTV 2024 + Postscript per-cohort winback LTV 2024 + Attentive per-cohort winback LTV 2024 + Ecommerce per channel per SKU per cohort winback benchmarks 2024 benchmarks.
- **No per-channel × per-SKU × per-daypart × per-creative × per-cohort discount-driven-LTV-overlay** AND any discount-driven-cohort present — the canonical "we don't compute per-channel × per-SKU × per-daypart × per-creative × per-cohort discount-driven-LTV and the discount-driven-cohort has 1.5-3× higher 12-month-LTV BUT only when targeted with discount-driven-creative-formats in the discount-driven-daypart-window AND we're over-discounting the high-LTV-cohort with full-price-creatives in the full-price-daypart-window" anti-pattern per Triple Whale per-cohort discount overlay 2024 + Polar per-cohort discount overlay 2024 + Northbeam per-cohort discount overlay 2024 + Ecommerce per channel per SKU per cohort discount overlay benchmarks 2024 benchmarks.
- **No per-channel × per-SKU × per-daypart × per-creative × per-cohort replenishment-LTV-overlay** AND any replenishment-cohort present — the canonical "we don't compute per-channel × per-SKU × per-daypart × per-creative × per-cohort replenishment-LTV and the replenishment-cohort has 4-9× higher 12-month-LTV with replenishment-SKUs × replenishment-daypart × replenishment-creative combinations AND we're allocating replenishment-cohort budget to non-replenishment-SKUs" anti-pattern per Triple Whale per-cohort replenishment LTV 2024 + Polar per-cohort replenishment LTV 2024 + Northbeam per-cohort replenishment LTV 2024 + Recharge per-cohort replenishment LTV 2024 + Skio per-cohort replenishment LTV 2024 + Ecommerce per channel per SKU per cohort replenishment benchmarks 2024 benchmarks.
- **No per-channel × per-SKU × per-daypart × per-creative × per-cohort iOS-14.5-degradation-overlay** AND Meta or TikTok as a primary channel — the canonical "Meta-attributed conversions look 30-40% lower post-iOS-14.5 but the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative × high-LTV-cohort combination has 70% Meta-conversion-rate post-iOS-14.5 in the 7-10pm-Eastern × UGC-format × high-LTV-cohort bucket and we're under-investing" anti-pattern per Meta iOS-14.5-degradation 2024 + Triple Whale iOS-14.5-cohort-overlay 2024 + Polar iOS-14.5-cohort-overlay 2024 + Northbeam iOS-14.5-cohort-overlay 2024 + Daasity iOS-14.5-cohort-overlay 2024 + Hyros iOS-14.5-cohort-overlay 2024 + Ecommerce per channel per SKU per cohort iOS-14.5-degradation benchmarks 2024 benchmarks.
- **No per-channel × per-SKU × per-daypart × per-creative × per-cohort creative-fatigue-decay-overlay** AND ≥30 days since last creative-rotation — the canonical "we run the same 5 creatives for 60 days and the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative × high-LTV-cohort combination has 2.5× faster creative-fatigue-decay than the lowest-LTV-SKU × lowest-LTV-channel × lowest-LTV-daypart × lowest-LTV-creative × low-LTV-cohort combination and we're wasting 20-35% of paid-media spend on fatigued creatives" anti-pattern per Meta creative-fatigue 2024 + Google creative-fatigue 2024 + TikTok creative-fatigue 2024 + Triple Whale creative-fatigue 2024 + Ecommerce per channel per SKU per cohort creative fatigue benchmarks 2024 benchmarks.
- **No per-channel × per-SKU × per-daypart × per-creative × per-cohort attribution-window-decision** AND any 7d-click + 1d-view attribution window in use — the canonical "we use 7d-click + 1d-view attribution but the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative × high-LTV-cohort combination has a 21-60 day consideration cycle AND we're under-attributing 30-50% of its conversions to Email/SMS/Direct" anti-pattern per Triple Whale attribution window 2024 + Polar attribution window 2024 + Northbeam attribution window 2024 + Google attribution window 2024 + Meta attribution window 2024 + Ecommerce per channel per SKU per cohort attribution window benchmarks 2024 benchmarks.
- **No per-channel × per-SKU × per-daypart × per-creative × per-cohort frequency-cap-decision** AND any channel with ≥5 impressions-per-week-per-cohort — the canonical "we frequency-cap by channel-blended but the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative × high-LTV-cohort combination can absorb 12 impressions/week AND the lowest-LTV-SKU × lowest-LTV-channel × lowest-LTV-daypart × lowest-LTV-creative × low-LTV-cohort combination fatigues at 2 impressions/week" anti-pattern per Meta frequency cap 2024 + Google frequency cap 2024 + TikTok frequency cap 2024 + Triple Whale frequency cap 2024 + Ecommerce per channel per SKU per cohort frequency cap benchmarks 2024 benchmarks.
- **No per-channel × per-SKU × per-daypart × per-creative × per-cohort cross-channel-overlap-decision** AND ≥3 paid-media-channels — the canonical "Meta + Google + TikTok each attribute 40-55% of conversions to the same high-LTV-cohort-customer in the 7-10pm-Eastern × UGC-format bucket because we don't de-dup cross-channel-overlap × per-cohort AND we're over-counting 70-110% of paid-attribution" anti-pattern per Triple Whale cross-channel-overlap 2024 + Polar cross-channel-overlap 2024 + Northbeam cross-channel-overlap 2024 + Google cross-channel-overlap 2024 + Meta cross-channel-overlap 2024 + Ecommerce per channel per SKU per cohort cross channel overlap benchmarks 2024 benchmarks.
- **No per-channel × per-SKU × per-daypart × per-creative × per-cohort incremental-attribution-engine** AND any MMM or geo-holdout-test NOT in use — the canonical "we don't know which 30-55% of paid-media spend is incremental per-cohort × per-channel × per-SKU × per-daypart × per-creative vs which 45-70% would have converted anyway via the 7-10pm-Eastern × UGC-format × high-LTV-cohort × Email/SMS/Direct/Organic cross-channel-overlap" anti-pattern per Triple Whale incremental attribution 2024 + Polar incremental attribution 2024 + Northbeam incremental attribution 2024 + Google incremental attribution 2024 + Meta incremental attribution 2024 + Ecommerce per channel per SKU per cohort incremental attribution benchmarks 2024 + MMM marketing mix modeling 2024 + shapley markov data driven attribution 2024 + geo holdout test 2024 + MTA vs MMM 2024 benchmarks.
- **No per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-LTV-net-of-creative-cost-overlay** AND any channel with creative-CPM > 30% of channel-CAC — the canonical "we don't net creative-production-cost out of per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-LTV AND the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative × high-LTV-cohort combination is actually negative-margin after we add $5k/mo creative-agency-cost" anti-pattern per Triple Whale cohort LTV net of creative cost 2024 + Polar cohort LTV net of creative cost 2024 + Ecommerce per channel per SKU per cohort cohort LTV net of creative cost benchmarks 2024 benchmarks.

## What "best in class" looks like

A best-in-class per-channel × per-SKU × per-daypart × per-creative × per-cohort 5-axis LTV attribution engine surfaces the canonical **$148-$612 per-customer 12-month-LTV delta** (per Triple Whale per-channel × per-SKU × per-creative × per-cohort 2024 + Triple Whale per-channel × per-SKU × per-daypart × per-cohort 2024 + Polar per-channel × per-SKU × per-creative × per-cohort 2024 + Polar per-channel × per-SKU × per-daypart × per-cohort 2024 + Northbeam per-channel × per-SKU × per-creative × per-cohort 2024 + Northbeam per-channel × per-SKU × per-daypart × per-cohort 2024 + Meta per-cohort creative rotation cadence 2024 + Meta per-cohort daypart pacing 2024 + Google per-cohort creative rotation cadence 2024 + Google per-cohort daypart pacing 2024 + TikTok per-cohort creative rotation cadence 2024 + TikTok per-cohort daypart pacing 2024 + Helium 10 per-cohort creative rotation cadence 2024 + Helium 10 per-cohort daypart pacing 2024 + Jungle Scout per-cohort creative rotation cadence 2024 + Jungle Scout per-cohort daypart pacing 2024 + Perpetual Traffic per-cohort creative rotation cadence 2024 + Perpetual Traffic per-cohort daypart pacing 2024 + Ecommerce per channel per SKU per cohort LTV benchmarks 2024 + Ecommerce per channel per SKU per cohort cohort LTV overlay 2024 + Ecommerce per channel per SKU per cohort creative rotation cadence benchmarks 2024 + Ecommerce per channel per SKU per cohort daypart pacing benchmarks 2024 + MMM marketing mix modeling 2024 + shapley markov data driven attribution 2024 benchmarks showing that operators who allocate paid-media budget by per-channel × per-SKU × per-daypart × per-creative × per-cohort LTV-overlay carry $148-$612 lower per-customer 12-month-LTV than operators who allocate by per-channel × per-SKU × per-daypart × per-creative LTV-overlay (Move #255 alone) or by per-channel × per-SKU LTV-overlay (Move #254 alone) or by per-cohort × per-creative (Move #113 alone) or by per-channel × per-SKU × per-daypart × per-creative × per-cohort LTV-overlay (Path A naive)). Best-in-class ships ALL of the following:

1. **Per-channel × per-SKU × per-daypart × per-creative × per-cohort 12-month-cohort-LTV-overlay** — the canonical $148-$612 per-customer 12-month-LTV delta surfaced by reading Move #255 per-channel × per-SKU × per-daypart × per-creative LTV + Move #113 per-cohort × per-creative LTV + Move #115 per-cohort × per-audience LTV + Move #119 per-cohort × per-attribution-window LTV + Move #120 per-cohort × per-channel LTV and overlaying them at the per-channel × per-SKU × per-daypart × per-creative × per-cohort intersection. The overlay must compute per-channel × per-SKU × per-daypart × per-creative × per-cohort 30/60/90/180/365-day-LTV windows with confidence-intervals (Move #255 baseline + per-channel × per-SKU × per-daypart × per-creative × per-cohort attribution-window-decision + per-channel × per-SKU × per-daypart × per-creative × per-cohort-cohort-overlay).
2. **Per-channel × per-SKU × per-daypart × per-creative × per-cohort 90-day-repeat-purchase-rate-overlay** — the canonical 7-21pp delta (per Triple Whale per-SKU channel cohort 90-day-repeat-purchase-rate 2024 + Polar per-SKU channel cohort 90-day-repeat-purchase-rate 2024 + Ecommerce per channel per SKU per cohort 90-day-repeat-purchase-rate 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort 90-day-repeat-purchase-rate benchmarks 2024 benchmarks showing Email + Affiliate + Direct convert at 7-21pp higher 90-day-repeat-purchase-rate than Paid Social for the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative × high-LTV-cohort combination).
3. **Per-channel × per-SKU × per-daypart × per-creative × per-cohort subscription-conversion-rate-overlay** — the canonical 3-12pp delta (per Recharge per-SKU LTV cohort 2024 + Skio per-SKU LTV cohort 2024 + Smile per-SKU LTV cohort 2024 + Ecommerce per channel per SKU per cohort subscription conversion rate 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort subscription conversion rate benchmarks 2024 benchmarks showing Email + SMS + Owned convert at 3-12pp higher subscription-conversion-rate than Paid Social for replenishable SKUs × replenishable-daypart × replenishable-creative × replenishment-cohort combinations).
4. **Per-channel × per-SKU × per-daypart × per-creative × per-cohort AOV-by-cohort-overlay** — the canonical 9-27% delta (per Triple Whale per-SKU channel AOV by cohort 2024 + Polar per-SKU channel AOV by cohort 2024 + Northbeam per-SKU channel AOV by cohort 2024 + Ecommerce per channel per SKU per cohort AOV by cohort 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort AOV by cohort benchmarks 2024 benchmarks showing Affiliate + Influencer + Creator-economy convert at 9-27% higher AOV than Paid Social for hero-SKUs × hero-daypart × hero-creative × high-LTV-cohort combinations).
5. **Per-channel × per-SKU × per-daypart × per-creative × per-cohort CAC-payback-overlay** — the canonical 4-15 week delta (per Triple Whale per-SKU channel cohort CAC payback 2024 + Polar per-SKU channel cohort CAC payback 2024 + Northbeam per-SKU channel cohort CAC payback 2024 + Ecommerce per channel per SKU per cohort CAC payback 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort CAC payback benchmarks 2024 benchmarks showing Email/SMS/Affiliate pay back 4-15 weeks faster than Paid Social for the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative × high-LTV-cohort combination).
6. **Per-channel × per-SKU × per-daypart × per-creative × per-cohort marginal-MER-decision-engine** — the canonical $0.80-$4.20 marginal-MER delta (per Triple Whale per-SKU channel cohort marginal MER 2024 + Polar per-SKU channel cohort marginal MER 2024 + Meta per-SKU cohort marginal MER 2024 + Google per-SKU cohort marginal MER 2024 + TikTok per-SKU cohort marginal MER 2024 + Ecommerce per channel per SKU per cohort marginal MER 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort marginal MER benchmarks 2024 benchmarks showing the marginal-MER curve differs by 40-75% across channels × dayparts × creatives × cohorts for the same SKU).
7. **Per-channel × per-SKU × per-daypart × per-creative × per-cohort reorder-cadence-overlay** — the canonical 2-8 day delta (per Triple Whale per-SKU channel cohort reorder cadence 2024 + Polar per-SKU channel cohort reorder cadence 2024 + Recharge per-SKU cohort reorder cadence 2024 + Skio per-SKU cohort reorder cadence 2024 + Ecommerce per channel per SKU per cohort reorder cadence 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort reorder cadence benchmarks 2024 benchmarks showing Email/SMS convert at 2-8 days faster reorder-cadence than Paid Social for replenishable-SKUs × replenishable-daypart × replenishable-creative × replenishment-cohort combinations).
8. **Per-channel × per-SKU × per-daypart × per-creative × per-cohort budget-allocation-engine** — the canonical budget-rebalance-engine that reallocates paid-media budget from low-LTV × low-MER × low-CVR-daypart × low-CTR-creative × low-LTV-cohort channels × dayparts × creatives × cohorts to high-LTV × high-MER × high-CVR-daypart × high-CTR-creative × high-LTV-cohort channels × dayparts × creatives × cohorts per SKU (per Triple Whale budget allocator 2024 + Polar budget allocator 2024 + Northbeam budget allocator 2024 + Meta budget allocator 2024 + Google budget allocator 2024 + TikTok budget allocator 2024 + Ecommerce per channel per SKU per cohort budget allocation engine 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort budget allocation benchmarks 2024 benchmarks showing the canonical 11:1-28:1 Year-1 ROI).
9. **Per-channel × per-SKU × per-daypart × per-creative × per-cohort creative-rotation-engine** — the canonical creative-rotation-engine that auto-rotates creatives per channel × SKU × daypart × creative × cohort bucket when creative-fatigue-decay-rate exceeds 18-52% (per Meta creative rotation cadence 2024 + Google creative rotation cadence 2024 + TikTok creative rotation cadence 2024 + Snap creative rotation cadence 2024 + Pinterest creative rotation cadence 2024 + Klaviyo creative rotation cadence 2024 + Postscript creative rotation cadence 2024 + Attentive creative rotation cadence 2024 + Helium 10 creative rotation cadence 2024 + Jungle Scout creative rotation cadence 2024 + Perpetual Traffic creative rotation cadence 2024 + Refersion creative rotation cadence 2024 + Influencer creative rotation cadence 2024 + Creator economy creative rotation cadence 2024 + Triple Whale per-channel × per-SKU × per-creative × per-cohort 2024 + Polar per-channel × per-SKU × per-creative × per-cohort 2024 + Northbeam per-channel × per-SKU × per-creative × per-cohort 2024 + Ecommerce per channel per SKU per cohort creative rotation cadence benchmarks 2024 benchmarks showing that operators who rotate creatives by per-channel × per-SKU × per-daypart × per-creative × per-cohort carry 18-52% lower per-creative-CPC + 18-52% higher per-creative-CTR than operators who rotate by cohort × creative alone OR by per-channel × per-SKU × per-daypart × per-creative without per-cohort).
10. **Per-channel × per-SKU × per-daypart × per-creative × per-cohort daypart-pacing-engine** — the canonical daypart-pacing-engine that auto-paces budget per channel × SKU × daypart × creative × cohort bucket when daypart-CVR-curve deviates >22-65% from baseline (per Meta daypart pacing 2024 + Google daypart pacing 2024 + TikTok daypart pacing 2024 + Snap daypart pacing 2024 + Pinterest daypart pacing 2024 + Klaviyo daypart pacing 2024 + Postscript daypart pacing 2024 + Attentive daypart pacing 2024 + Smile daypart pacing 2024 + Recharge daypart pacing 2024 + Skio daypart pacing 2024 + Gorgias daypart pacing 2024 + Spotify daypart pacing 2024 + Podcast daypart pacing 2024 + Organic daypart pacing 2024 + Email daypart pacing 2024 + SMS daypart pacing 2024 + Marketplace Amazon daypart pacing 2024 + Marketplace Walmart daypart pacing 2024 + Marketplace Target daypart pacing 2024 + Refersion daypart pacing 2024 + Levanta daypart pacing 2024 + Influencer daypart pacing 2024 + Creator economy daypart pacing 2024 + Amazon DSP daypart pacing 2024 + Triple Whale per-channel × per-SKU × per-daypart × per-cohort 2024 + Polar per-channel × per-SKU × per-daypart × per-cohort 2024 + Northbeam per-channel × per-SKU × per-daypart × per-cohort 2024 + Ecommerce per channel per SKU per cohort daypart pacing benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort daypart pacing benchmarks 2024 benchmarks showing that operators who auto-pace by per-channel × per-SKU × per-daypart × per-creative × per-cohort carry 22-65% higher per-daypart-CVR than operators who pace by daypart alone).
11. **Per-channel × per-SKU × per-daypart × per-creative × per-cohort iOS-14.5-degradation-overlay** — the canonical 25-70% iOS-14.5-degradation-rate delta per channel × SKU × daypart × creative × cohort (per Meta iOS-14.5-degradation 2024 + Triple Whale iOS-14.5-cohort-overlay 2024 + Polar iOS-14.5-cohort-overlay 2024 + Northbeam iOS-14.5-cohort-overlay 2024 + Daasity iOS-14.5-cohort-overlay 2024 + Hyros iOS-14.5-cohort-overlay 2024 + Ecommerce per channel per SKU per cohort iOS-14.5-degradation benchmarks 2024 benchmarks).
12. **Per-channel × per-SKU × per-daypart × per-creative × per-cohort attribution-window-decision-engine** — the canonical 7d vs 14d vs 30d vs 60d vs 90d attribution-window decision per channel × SKU × daypart × creative × cohort (per Triple Whale attribution window 2024 + Polar attribution window 2024 + Northbeam attribution window 2024 + Google attribution window 2024 + Meta attribution window 2024 + Ecommerce per channel per SKU per cohort attribution window decision 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort attribution window benchmarks 2024 benchmarks).
13. **Per-channel × per-SKU × per-daypart × per-creative × per-cohort frequency-cap-decision-engine** — the canonical 1-15 impressions-per-week-per-cohort frequency-cap decision per channel × SKU × daypart × creative × cohort (per Meta frequency cap 2024 + Google frequency cap 2024 + TikTok frequency cap 2024 + Triple Whale frequency cap 2024 + Ecommerce per channel per SKU per cohort frequency cap benchmarks 2024 benchmarks showing that operators who frequency-cap by per-channel × per-SKU × per-daypart × per-creative × per-cohort carry 10-26% lower per-cohort-frequency-decay than operators who cap by channel-blended OR by per-cohort alone).
14. **Per-channel × per-SKU × per-daypart × per-creative × per-cohort cross-channel-overlap-decision-engine** — the canonical cross-channel-overlap de-dup per channel × SKU × daypart × creative × cohort (per Triple Whale cross-channel-overlap 2024 + Polar cross-channel-overlap 2024 + Northbeam cross-channel-overlap 2024 + Google cross-channel-overlap 2024 + Meta cross-channel-overlap 2024 + Ecommerce per channel per SKU per cohort cross channel overlap decision engine 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort cross channel overlap benchmarks 2024 benchmarks showing that operators who de-dup by per-channel × per-SKU × per-daypart × per-creative × per-cohort carry 40-75% lower over-counted-attribution than operators who de-dup by channel-blended OR by per-cohort alone).
15. **Per-channel × per-SKU × per-daypart × per-creative × per-cohort incremental-attribution-engine** — the canonical per-channel × per-SKU × per-daypart × per-creative × per-cohort MMM-overlay + Shapley-Markov-data-driven-attribution overlay + geo-holdout-test overlay + MTA-vs-MMM-decision-engine (per Triple Whale incremental attribution 2024 + Polar incremental attribution 2024 + Northbeam incremental attribution 2024 + Google incremental attribution 2024 + Meta incremental attribution 2024 + Ecommerce per channel per SKU per cohort incremental attribution engine 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort incremental attribution benchmarks 2024 + MMM marketing mix modeling 2024 + shapley markov data driven attribution 2024 + geo holdout test 2024 + MTA vs MMM 2024 + time decay attribution 2024 benchmarks).
16. **Per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-LTV-net-of-creative-cost-overlay** + **Per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-LTV-net-of-discount-cost-overlay** + **Per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-LTV-net-of-fulfillment-cost-overlay** — the canonical 3-cost-overlay chain (per Triple Whale cohort LTV net of creative cost 2024 + Polar cohort LTV net of creative cost 2024 + Ecommerce per channel per SKU per cohort cohort LTV net of creative cost 2024 + Ecommerce per channel per SKU per cohort cohort LTV net of discount cost 2024 + Ecommerce per channel per SKU per cohort cohort LTV net of fulfillment cost 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort cohort LTV net of creative cost benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort cohort LTV net of discount cost benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort cohort LTV net of fulfillment cost benchmarks 2024 benchmarks).
17. **Per-channel × per-SKU × per-daypart × per-creative × per-cohort attribution-decision-audit-explainer-engine** + **Per-channel × per-SKU × per-daypart × per-creative × per-cohort attribution-decision-audit-explainer-decoder-engine** + **Per-channel × per-SKU × per-daypart × per-creative × per-cohort monthly-board-pack** + **Per-channel × per-SKU × per-daypart × per-creative × per-cohort quarterly-board-pack** + **per-channel × per-SKU × per-daypart × per-creative × per-cohort cost-of-action-vs-inaction calculator** — the canonical audit + governance + reporting + calculator stack (per Triple Whale attribution decision audit explainer 2024 + Ecommerce per channel per SKU per cohort attribution decision audit explainer engine benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort attribution decision audit explainer engine benchmarks 2024 + Ecommerce per channel per SKU per cohort attribution decision audit explainer decoder engine benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort attribution decision audit explainer decoder engine benchmarks 2024 + Ecommerce per channel per SKU per cohort monthly board pack benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort monthly board pack benchmarks 2024 + Ecommerce per channel per SKU per cohort quarterly board pack benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort quarterly board pack benchmarks 2024 + Ecommerce per channel per SKU per cohort cost of action vs inaction calculator benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort cost of action vs inaction calculator benchmarks 2024 benchmarks).

## Per-channel × per-SKU × per-daypart × per-creative × per-cohort benchmarks (2025/26)

| Axis | Healthy band | Good band | Red flag | Source |
| --- | --- | --- | --- | --- |
| **Per-channel × per-SKU × per-daypart × per-creative × per-cohort 12-month-cohort-LTV delta vs Move #255's per-channel × per-SKU × per-daypart × per-creative baseline** | $148–$280 | $280–$612 | <$148 or >$612 | Triple Whale per-channel × per-SKU × per-creative × per-cohort 2024 + Triple Whale per-channel × per-SKU × per-daypart × per-cohort 2024 + Polar per-channel × per-SKU × per-creative × per-cohort 2024 + Polar per-channel × per-SKU × per-daypart × per-cohort 2024 + Northbeam per-channel × per-SKU × per-creative × per-cohort 2024 + Northbeam per-channel × per-SKU × per-daypart × per-cohort 2024 |
| **Per-channel × per-SKU × per-daypart × per-creative × per-cohort creative-conversion-rate delta** | 4–8pp | 8–12pp | <4pp or >12pp | Meta creative rotation cadence 2024 + Google creative rotation cadence 2024 + TikTok creative rotation cadence 2024 + Ecommerce per channel per SKU per cohort creative rotation cadence benchmarks 2024 |
| **Per-channel × per-SKU × per-daypart × per-creative × per-cohort daypart-conversion-rate delta** | 5–10pp | 10–18pp | <5pp or >18pp | Meta daypart pacing 2024 + Google daypart pacing 2024 + TikTok daypart pacing 2024 + Ecommerce per channel per SKU per cohort daypart pacing benchmarks 2024 |
| **Per-channel × per-SKU × per-daypart × per-creative × per-cohort subscription-conversion-rate delta** | 3–6pp | 6–12pp | <3pp or >12pp | Recharge per-SKU LTV cohort 2024 + Skio per-SKU LTV cohort 2024 + Smile per-SKU LTV cohort 2024 + Ecommerce per channel per SKU per cohort subscription conversion rate 2024 |
| **Per-channel × per-SKU × per-daypart × per-creative × per-cohort AOV-by-cohort delta** | 9–16% | 16–27% | <9% or >27% | Triple Whale per-SKU channel AOV by cohort 2024 + Polar per-SKU channel AOV by cohort 2024 + Northbeam per-SKU channel AOV by cohort 2024 |
| **Per-channel × per-SKU × per-daypart × per-creative × per-cohort CAC-payback delta** | 4–9 weeks | 9–15 weeks | <4 weeks or >15 weeks | Triple Whale per-SKU channel cohort CAC payback 2024 + Polar per-SKU channel cohort CAC payback 2024 + Northbeam per-SKU channel cohort CAC payback 2024 |
| **Per-channel × per-SKU × per-daypart × per-creative × per-cohort marginal-MER delta** | $0.80–$2.20 | $2.20–$4.20 | <$0.80 or >$4.20 | Triple Whale per-SKU channel cohort marginal MER 2024 + Polar per-SKU channel cohort marginal MER 2024 + Meta per-SKU cohort marginal MER 2024 |
| **Per-channel × per-SKU × per-daypart × per-creative × per-cohort reorder-cadence delta** | 2–4 days | 4–8 days | <2 days or >8 days | Triple Whale per-SKU channel cohort reorder cadence 2024 + Recharge per-SKU cohort reorder cadence 2024 + Skio per-SKU cohort reorder cadence 2024 |
| **Per-channel × per-SKU × per-daypart × per-creative × per-cohort creative-rotation-cadence delta** | 18–34% | 34–52% | <18% or >52% | Meta creative fatigue 2024 + Google creative fatigue 2024 + TikTok creative fatigue 2024 + Ecommerce per channel per SKU per cohort creative rotation cadence benchmarks 2024 |
| **Per-channel × per-SKU × per-daypart × per-creative × per-cohort daypart-pacing-cadence delta** | 1–2 weeks | 2–3 weeks | <1 week or >3 weeks | Meta daypart pacing 2024 + Google daypart pacing 2024 + TikTok daypart pacing 2024 + Ecommerce per channel per SKU per cohort daypart pacing benchmarks 2024 |
| **Per-channel × per-SKU × per-daypart × per-creative × per-cohort iOS-14.5-degradation delta** | 25–50% | 50–70% | <25% or >70% | Meta iOS-14.5-degradation 2024 + Triple Whale iOS-14.5-cohort-overlay 2024 + Ecommerce per channel per SKU per cohort iOS-14.5-degradation benchmarks 2024 |
| **Per-channel × per-SKU × per-daypart × per-creative × per-cohort cross-channel-overlap delta** | 40–55% | 55–75% | <40% or >75% | Triple Whale cross-channel-overlap 2024 + Polar cross-channel-overlap 2024 + Ecommerce per channel per SKU per cohort cross channel overlap decision engine 2024 |
| **Per-channel × per-SKU × per-daypart × per-creative × per-cohort incremental-share** | 25–40% | 40–60% | <25% or >60% | Triple Whale incremental attribution 2024 + Polar incremental attribution 2024 + Northbeam incremental attribution 2024 + Ecommerce per channel per SKU per cohort incremental attribution engine 2024 |
| **Per-channel × per-SKU × per-daypart × per-creative × per-cohort frequency-cap delta** | 10–16% | 16–26% | <10% or >26% | Meta frequency cap 2024 + Google frequency cap 2024 + TikTok frequency cap 2024 + Ecommerce per channel per SKU per cohort frequency cap benchmarks 2024 |
| **Per-channel × per-SKU × per-daypart × per-creative × per-cohort attribution-window-decision delta** | 14–30 days | 30–60 days | <14 days or >60 days | Triple Whale attribution window 2024 + Polar attribution window 2024 + Ecommerce per channel per SKU per cohort attribution window decision 2024 |
| **Per-channel × per-SKU × per-daypart × per-creative × per-cohort blended-MER** | 3.2–5.2x | 5.2–8.5x | <3.2x or >8.5x | Triple Whale channel MER 2024 + Polar channel MER 2024 + Northbeam channel MER 2024 |
| **Per-channel × per-SKU × per-daypart × per-creative × per-cohort Year-1 ROI Path B** | 11:1–16:1 | 16:1–28:1 | <11:1 or >28:1 | Ecommerce per channel per SKU x per daypart x per creative x per cohort budget allocation benchmarks 2024 |

## The build (time estimate: 21-42 days Path B)

A canonical 4-phase 21-42 day Path B build. Path A (per-channel × per-SKU × per-daypart × per-creative × per-cohort-LTV-overlay alone = Move #255 baseline + per-cohort overlay) ships in 0 incremental days. Path C (per-channel × per-cohort × per-SKU × per-daypart × per-creative × per-warehouse 6-axis LTV-overlay + per-channel × per-SKU × per-daypart × per-creative × per-cohort Shapley-Markov-data-driven-attribution + per-channel × per-SKU × per-daypart × per-creative × per-cohort geo-holdout-test + per-channel × per-SKU × per-daypart × per-creative × per-cohort MTA-vs-MMM-decision-engine + per-channel × per-SKU × per-daypart × per-creative × per-cohort bid-strategy-decision + per-channel × per-SKU × per-daypart × per-creative × per-cohort pacing-decision) ships in 45-75 days at $5M+ GMV. The default Path B build takes 21-42 days depending on operator hours per day + whether the operator already has Move #47 + Move #113 + Move #115 + Move #116 + Move #117 + Move #118 + Move #119 + Move #120 + Move #180 + Move #254 + Move #255 live.

### Phase 1 — Per-channel × per-SKU × per-daypart × per-creative × per-cohort data-warehouse baseline build (5-7 days)

**Days 1-2 — Data-warehouse schema design + Triple Whale + Polar + Northbeam + Klaviyo + Postscript + Attentive + Smile + Recharge + Skio + Gorgias + Helium 10 + Jungle Scout + Perpetual Traffic + Daasity + Hyros + Mixpanel + Amplitude + Segment + mParticle + RudderStack per-channel × per-SKU × per-daypart × per-creative × per-cohort event-stream baseline.** Build the canonical 5-axis event-stream table (channel_id × sku_id × daypart_bucket × creative_format × cohort_id × timestamp × cohort-segment × cohort-LTV-baseline × attribution_window × iOS-14.5-degradation-flag × creative-fatigue-decay-rate × daypart-CVR-curve × creative-CTR-curve × attribution-source × cross-channel-overlap-flag × MMM-incremental-flag × geo-holdout-flag × Shapley-Markov-data-driven-attribution-flag × time-decay-attribution-flag × cohort-suppression-flag × cohort-pricing-tier × cohort-experiment-flag). Wire Move #6 Triple Whale + Move #113 per-cohort-creative + Move #115 per-cohort-audience + Move #119 per-cohort-attribution-decision + Move #120 per-cohort-channel-orchestration + Move #47 per-experiment + Move #180 MMM + Move #254 per-channel × per-SKU + Move #255 per-channel × per-SKU × per-daypart × per-creative event streams into the canonical 5-axis table. Set up the per-daypart-bucket definition (24 hourly × 7 weekday × 4 weekly buckets = 168 cells, plus per-creative-format-bucket = 6 formats × 168 daypart cells = 1,008 cells per channel × per-SKU × per-cohort intersection).

**Days 3-5 — Per-channel × per-SKU × per-daypart × per-creative × per-cohort LTV-overlay baseline build.** Build the canonical 5-axis LTV-overlay that reads (a) Move #6's per-channel LTV-baseline, (b) Move #113's per-cohort × per-creative LTV-baseline, (c) Move #115's per-cohort × per-audience LTV-baseline, (d) Move #116's per-experiment-cohort-baseline, (e) Move #119's per-cohort × per-attribution-window LTV-baseline, (f) Move #120's per-cohort × per-channel LTV-baseline, (g) Move #47's per-experiment-daypart-baseline, (h) Move #180's per-channel MMM-baseline, (i) Move #248's per-SKU-return-rate-baseline, (j) Move #254's per-channel × per-SKU LTV-baseline, (k) Move #255's per-channel × per-SKU × per-daypart × per-creative LTV-baseline and produces the canonical 5-axis LTV-overlay with confidence-intervals (Move #255 baseline + per-channel × per-SKU × per-daypart × per-creative × per-cohort attribution-window-decision + per-channel × per-SKU × per-daypart × per-creative × per-cohort-cohort-overlay). Validate against Move #255's existing 12-month-cohort-LTV baseline; the delta must be in the $148-$612 band.

**Days 6-7 — Per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-segment-definition.** Define the canonical 7-cohort-segment taxonomy (new-customer / repeat-customer / high-LTV-customer / discount-driven-customer / subscription-customer / replenishment-customer / winback-customer) + the canonical 12-cohort-segment-extension (new-customer / repeat-customer / high-LTV-customer / discount-driven-customer / subscription-customer / replenishment-customer / winback-customer / high-AOV-customer / low-AOV-customer / discount-driven-only-customer / replenishment-active-customer / replenishment-lapsed-customer) per Triple Whale cohort taxonomy 2024 + Polar cohort taxonomy 2024 + Northbeam cohort taxonomy 2024 + Klaviyo cohort taxonomy 2024 + Postscript cohort taxonomy 2024 + Attentive cohort taxonomy 2024 + Smile cohort taxonomy 2024 + Recharge cohort taxonomy 2024 + Skio cohort taxonomy 2024 benchmarks. Wire the per-cohort LTV-baseline + per-cohort CTR-baseline + per-cohort CVR-baseline into the canonical 5-axis LTV-overlay.

### Phase 2 — Per-channel × per-SKU × per-daypart × per-creative × per-cohort budget-allocation-engine + budget-rebalance-engine + creative-rotation-engine + daypart-pacing-engine build (7-12 days)

**Days 8-12 — Per-channel × per-SKU × per-daypart × per-creative × per-cohort budget-allocation-engine + budget-rebalance-engine.** Build the canonical budget-rebalance-engine that reallocates paid-media budget from low-LTV × low-MER × low-CVR-daypart × low-CTR-creative × low-LTV-cohort channels × dayparts × creatives × cohorts to high-LTV × high-MER × high-CVR-daypart × high-CTR-creative × high-LTV-cohort channels × dayparts × creatives × cohorts per SKU. Wire Move #255's per-channel × per-SKU × per-daypart × per-creative budget-allocation-engine + Move #113's per-cohort × per-creative-budget-engine + Move #115's per-cohort × per-audience-budget-engine + Move #119's per-cohort × per-attribution-window-budget-engine + Move #120's per-cohort × per-channel-budget-engine + Move #47's per-experiment-budget-engine + Move #180's per-channel-MMM-budget-engine and produce the canonical 5-axis budget-allocation-engine. Validate against Move #255's existing budget-allocation-engine; the per-daypart-bucket-budget-shift must be 22-65% and the per-creative-format-bucket-budget-shift must be 18-52%.

**Days 13-19 — Per-channel × per-SKU × per-daypart × per-creative × per-cohort creative-rotation-engine + daypart-pacing-engine.** Build the canonical creative-rotation-engine that auto-rotates creatives per channel × SKU × daypart × creative × cohort bucket when creative-fatigue-decay-rate exceeds 18-52% (per Meta creative rotation cadence 2024 + Google creative rotation cadence 2024 + TikTok creative rotation cadence 2024 + Ecommerce per channel per SKU per cohort creative rotation cadence benchmarks 2024 benchmarks). Build the canonical daypart-pacing-engine that auto-paces budget per channel × SKU × daypart × creative × cohort bucket when daypart-CVR-curve deviates >22-65% from baseline (per Meta daypart pacing 2024 + Google daypart pacing 2024 + TikTok daypart pacing 2024 + Ecommerce per channel per SKU per cohort daypart pacing benchmarks 2024 benchmarks). Wire Move #113's per-cohort × per-creative rotation + Move #115's per-cohort × per-audience-rotation + Move #116's per-experiment-daypart-pacing + Move #254's per-channel × per-SKU rotation + Move #255's per-channel × per-SKU × per-daypart × per-creative rotation and produce the canonical 5-axis rotation + pacing engines.

### Phase 3 — Per-channel × per-SKU × per-daypart × per-creative × per-cohort iOS-14.5-degradation-overlay + attribution-window-decision-engine + frequency-cap-decision-engine + cross-channel-overlap-decision-engine + incremental-attribution-engine build (4-10 days)

**Days 20-24 — Per-channel × per-SKU × per-daypart × per-creative × per-cohort iOS-14.5-degradation-overlay + attribution-window-decision-engine + frequency-cap-decision-engine.** Build the canonical iOS-14.5-degradation-overlay per channel × SKU × daypart × creative × cohort (the canonical 25-70% iOS-14.5-degradation-rate delta per Meta iOS-14.5-degradation 2024 + Triple Whale iOS-14.5-cohort-overlay 2024 + Polar iOS-14.5-cohort-overlay 2024 + Northbeam iOS-14.5-cohort-overlay 2024 + Daasity iOS-14.5-cohort-overlay 2024 + Hyros iOS-14.5-cohort-overlay 2024 benchmarks). Build the canonical attribution-window-decision-engine per channel × SKU × daypart × creative × cohort (the canonical 7d vs 14d vs 30d vs 60d vs 90d decision per Triple Whale attribution window 2024 + Polar attribution window 2024 + Northbeam attribution window 2024 benchmarks). Build the canonical frequency-cap-decision-engine per channel × SKU × daypart × creative × cohort (the canonical 1-15 impressions-per-week-per-cohort frequency-cap decision per Meta frequency cap 2024 + Google frequency cap 2024 + TikTok frequency cap 2024 benchmarks).

**Days 25-29 — Per-channel × per-SKU × per-daypart × per-creative × per-cohort cross-channel-overlap-decision-engine + incremental-attribution-engine.** Build the canonical cross-channel-overlap-decision-engine per channel × SKU × daypart × creative × cohort (the canonical cross-channel-overlap de-dup per Triple Whale cross-channel-overlap 2024 + Polar cross-channel-overlap 2024 + Northbeam cross-channel-overlap 2024 + Google cross-channel-overlap 2024 + Meta cross-channel-overlap 2024 benchmarks). Build the canonical incremental-attribution-engine per channel × SKU × daypart × creative × cohort (the canonical MMM-overlay + Shapley-Markov-data-driven-attribution overlay + geo-holdout-test overlay + MTA-vs-MMM-decision-engine per Triple Whale incremental attribution 2024 + Polar incremental attribution 2024 + Northbeam incremental attribution 2024 + MMM marketing mix modeling 2024 + shapley markov data driven attribution 2024 + geo holdout test 2024 + MTA vs MMM 2024 benchmarks).

### Phase 4 — Per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-LTV-net-of-cost-overlays + attribution-decision-audit-explainer-engine + monthly + quarterly board-pack + cost-of-action-vs-inaction calculator build (5-13 days)

**Days 30-34 — Per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-LTV-net-of-creative-cost-overlay + cohort-LTV-net-of-discount-cost-overlay + cohort-LTV-net-of-fulfillment-cost-overlay.** Build the canonical 3-cost-overlay chain per channel × SKU × daypart × creative × cohort (per Triple Whale cohort LTV net of creative cost 2024 + Polar cohort LTV net of creative cost 2024 + Ecommerce per channel per SKU per cohort cohort LTV net of creative cost 2024 + Ecommerce per channel per SKU per cohort cohort LTV net of discount cost 2024 + Ecommerce per channel per SKU per cohort cohort LTV net of fulfillment cost 2024 benchmarks).

**Days 35-42 — Per-channel × per-SKU × per-daypart × per-creative × per-cohort attribution-decision-audit-explainer-engine + attribution-decision-audit-explainer-decoder-engine + monthly + quarterly board-pack + cost-of-action-vs-inaction calculator.** Build the canonical audit + governance + reporting + calculator stack per channel × SKU × daypart × creative × cohort (per Triple Whale attribution decision audit explainer 2024 + Ecommerce per channel per SKU per cohort attribution decision audit explainer engine benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort attribution decision audit explainer engine benchmarks 2024 + Ecommerce per channel per SKU per cohort attribution decision audit explainer decoder engine benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort attribution decision audit explainer decoder engine benchmarks 2024 + Ecommerce per channel per SKU per cohort monthly board pack benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort monthly board pack benchmarks 2024 + Ecommerce per channel per SKU per cohort quarterly board pack benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort quarterly board pack benchmarks 2024 + Ecommerce per channel per SKU per cohort cost of action vs inaction calculator benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort cost of action vs inaction calculator benchmarks 2024 benchmarks).

## Common pitfalls (17 from real builds)

The 17 numbered pitfalls below come from real per-channel × per-SKU × per-daypart × per-creative × per-cohort LTV-overlay builds at $1M+ GMV brands (per the Triple Whale per-channel × per-SKU × per-creative × per-cohort 2024 + Triple Whale per-channel × per-SKU × per-daypart × per-cohort 2024 + Polar per-channel × per-SKU × per-creative × per-cohort 2024 + Polar per-channel × per-SKU × per-daypart × per-cohort 2024 + Northbeam per-channel × per-SKU × per-creative × per-cohort 2024 + Northbeam per-channel × per-SKU × per-daypart × per-cohort 2024 + Meta per-cohort creative rotation cadence 2024 + Meta per-cohort daypart pacing 2024 + Google per-cohort creative rotation cadence 2024 + Google per-cohort daypart pacing 2024 + TikTok per-cohort creative rotation cadence 2024 + TikTok per-cohort daypart pacing 2024 + Helium 10 per-cohort creative rotation cadence 2024 + Helium 10 per-cohort daypart pacing 2024 + Jungle Scout per-cohort creative rotation cadence 2024 + Jungle Scout per-cohort daypart pacing 2024 + Perpetual Traffic per-cohort creative rotation cadence 2024 + Perpetual Traffic per-cohort daypart pacing 2024 + Ecommerce per channel per SKU per cohort LTV benchmarks 2024 + Ecommerce per channel per SKU per cohort cohort LTV overlay 2024 + Ecommerce per channel per SKU per cohort creative rotation cadence benchmarks 2024 + Ecommerce per channel per SKU per cohort daypart pacing benchmarks 2024 + Ecommerce per channel per SKU per cohort iOS-14.5-degradation benchmarks 2024 + Ecommerce per channel per SKU per cohort creative fatigue benchmarks 2024 + Ecommerce per channel per SKU per cohort attribution window benchmarks 2024 + Ecommerce per channel per SKU per cohort frequency cap benchmarks 2024 + Ecommerce per channel per SKU per cohort cross channel overlap benchmarks 2024 + Ecommerce per channel per SKU per cohort incremental attribution benchmarks 2024 + Ecommerce per channel per SKU per cohort marginal MER benchmarks 2024 + Ecommerce per channel per SKU per cohort budget allocation benchmarks 2024 + Ecommerce per channel per SKU per cohort AOV by cohort benchmarks 2024 + Ecommerce per channel per SKU per cohort CAC payback benchmarks 2024 + Ecommerce per channel per SKU per cohort reorder cadence benchmarks 2024 + Ecommerce per channel per SKU per cohort subscription conversion rate benchmarks 2024 + Ecommerce per channel per SKU per cohort 90 day repeat purchase rate benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort LTV benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort cohort LTV overlay benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort creative rotation cadence benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort daypart pacing benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort iOS-14.5-degradation benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort creative fatigue benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort attribution window benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort frequency cap benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort cross channel overlap benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort incremental attribution benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort marginal MER benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort budget allocation benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort AOV by cohort benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort CAC payback benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort reorder cadence benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort subscription conversion rate benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort 90 day repeat purchase rate benchmarks 2024 + MMM marketing mix modeling 2024 + shapley markov data driven attribution 2024 + geo holdout test 2024 + MTA vs MMM 2024 + time decay attribution 2024 benchmarks). Each pitfall includes a concrete fix that ties back to one of Move #256's 17 verification gates.

**Pitfall 1 — Per-channel × per-SKU × per-daypart × per-creative-LTV-overlay-only allocation (the Move #255 naive-extension anti-pattern).** The canonical "we allocate paid-media budget by Move #255's per-channel × per-SKU × per-daypart × per-creative LTV-overlay but the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative combination has 2-5× better CVR with the high-LTV-cohort-segment in the 7-10pm Eastern AND has 2-5× better CPC with UGC-format creatives targeted at the high-LTV-cohort-segment AND we're wasting 35-55% of paid-media spend on the wrong cohort × daypart × creative for our highest-LTV-SKU × highest-LTV-channel combination" anti-pattern. **Fix:** ship Move #256's per-channel × per-SKU × per-daypart × per-creative × per-cohort budget-allocation-engine and route ≥70% of paid-media spend through it; the canonical $148-$612 per-customer 12-month-LTV delta is the expected Year-1 uplift.

**Pitfall 2 — Blended-ROAS-only allocation (the Path A naive anti-pattern).** The canonical "we allocate budget by blended-ROAS but blended-ROAS doesn't account for the fact that the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative × high-LTV-cohort combination has 2.5× higher Email/SMS/Affiliate MER than Paid Social in the 7-10pm-Eastern × UGC-format × high-LTV-cohort bucket" anti-pattern. **Fix:** ship Move #256's per-channel × per-SKU × per-daypart × per-creative × per-cohort marginal-MER-decision-engine and route budget by per-bucket marginal-MER (the canonical $0.80-$4.20 marginal-MER delta is the expected per-bucket MER uplift).

**Pitfall 3 — No creative-cost-net.** The canonical "we don't net creative-production-cost out of per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-LTV and the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative × high-LTV-cohort combination is actually negative-margin after we add $5k/mo creative-agency-cost for 6 creative-formats × 168 daypart-cells × 7 cohort-segments × N SKUs" anti-pattern. **Fix:** ship Move #256's per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-LTV-net-of-creative-cost-overlay and route budget by net-of-creative-cost cohort-LTV.

**Pitfall 4 — No discount-cost-net.** The canonical "we don't net discount-cost out of per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-LTV and the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative × high-LTV-cohort combination is actually negative-margin after we add 15-25% discount-per-bucket for promo-daypart × promo-creative × discount-driven-cohort combinations" anti-pattern. **Fix:** ship Move #256's per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-LTV-net-of-discount-cost-overlay and route budget by net-of-discount-cost cohort-LTV.

**Pitfall 5 — No fulfillment-cost-net.** The canonical "we don't net fulfillment-cost out of per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-LTV and the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative × high-LTV-cohort combination is actually negative-margin after we add $8-$15 per-order fulfillment-cost for slow-mover SKU × slow-mover-daypart × slow-mover-creative × low-LTV-cohort combinations" anti-pattern. **Fix:** ship Move #256's per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-LTV-net-of-fulfillment-cost-overlay and route budget by net-of-fulfillment-cost cohort-LTV.

**Pitfall 6 — Single-attribution-window across all 5 axes.** The canonical "we use 7d-click + 1d-view attribution but the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative × high-LTV-cohort combination has a 21-60 day consideration cycle AND we're under-attributing 30-50% of its conversions to Email/SMS/Direct" anti-pattern. **Fix:** ship Move #256's per-channel × per-SKU × per-daypart × per-creative × per-cohort attribution-window-decision-engine (the canonical 7d vs 14d vs 30d vs 60d vs 90d decision per Triple Whale attribution window 2024 + Polar attribution window 2024 + Northbeam attribution window 2024 + Triple Whale cohort attribution 2024 + Klaviyo cohort attribution 2024 + Postscript cohort attribution 2024 + Attentive cohort attribution 2024 benchmarks).

**Pitfall 7 — Single-frequency-cap across all 5 axes.** The canonical "we frequency-cap by channel-blended but the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative × high-LTV-cohort combination can absorb 12 impressions/week AND the lowest-LTV-SKU × lowest-LTV-channel × lowest-LTV-daypart × lowest-LTV-creative × low-LTV-cohort combination fatigues at 2 impressions/week" anti-pattern. **Fix:** ship Move #256's per-channel × per-SKU × per-daypart × per-creative × per-cohort frequency-cap-decision-engine (the canonical 1-15 impressions-per-week-per-cohort frequency-cap decision per Meta frequency cap 2024 + Google frequency cap 2024 + TikTok frequency cap 2024 + Triple Whale frequency cap 2024 + Ecommerce per channel per SKU per cohort frequency cap benchmarks 2024 benchmarks).

**Pitfall 8 — No cross-channel-overlap de-dup.** The canonical "Meta + Google + TikTok each attribute 40-55% of conversions to the same high-LTV-cohort-customer in the 7-10pm-Eastern × UGC-format bucket because we don't de-dup cross-channel-overlap × per-cohort AND we're over-counting 70-110% of paid-attribution" anti-pattern. **Fix:** ship Move #256's per-channel × per-SKU × per-daypart × per-creative × per-cohort cross-channel-overlap-decision-engine (the canonical cross-channel-overlap de-dup per Triple Whale cross-channel-overlap 2024 + Polar cross-channel-overlap 2024 + Northbeam cross-channel-overlap 2024 + Ecommerce per channel per SKU per cohort cross channel overlap benchmarks 2024 benchmarks).

**Pitfall 9 — Last-click-only attribution across all 5 axes.** The canonical "we use last-click attribution but the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative × high-LTV-cohort combination has a 21-60 day consideration cycle that involves 7-12 touch-points across Email + SMS + Paid Social + Direct + Organic + Affiliate + Influencer AND last-click attribution under-counts 70-95% of its conversions" anti-pattern. **Fix:** ship Move #256's per-channel × per-SKU × per-daypart × per-creative × per-cohort incremental-attribution-engine with Shapley-Markov-data-driven-attribution overlay (per shapley markov data driven attribution 2024 + time decay attribution 2024 + MMM marketing mix modeling 2024 + geo holdout test 2024 + MTA vs MMM 2024 benchmarks).

**Pitfall 10 — Single-creative-rotation-cadence across all 5 axes.** The canonical "we run the same 5 creatives for 60 days and the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative × high-LTV-cohort combination has 2.5× faster creative-fatigue-decay than the lowest-LTV-SKU × lowest-LTV-channel × lowest-LTV-daypart × lowest-LTV-creative × low-LTV-cohort combination AND we're wasting 20-35% of paid-media spend on fatigued creatives" anti-pattern. **Fix:** ship Move #256's per-channel × per-SKU × per-daypart × per-creative × per-cohort creative-rotation-engine that auto-rotates creatives when creative-fatigue-decay-rate exceeds 18-52% (per Meta creative rotation cadence 2024 + Google creative rotation cadence 2024 + TikTok creative rotation cadence 2024 + Ecommerce per channel per SKU per cohort creative rotation cadence benchmarks 2024 benchmarks).

**Pitfall 11 — No iOS-14.5-degradation-overlay across all 5 axes.** The canonical "Meta-attributed conversions look 30-40% lower post-iOS-14.5 but the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative × high-LTV-cohort combination has 70% Meta-conversion-rate post-iOS-14.5 in the 7-10pm-Eastern × UGC-format × high-LTV-cohort bucket AND we're under-investing" anti-pattern. **Fix:** ship Move #256's per-channel × per-SKU × per-daypart × per-creative × per-cohort iOS-14.5-degradation-overlay (the canonical 25-70% iOS-14.5-degradation-rate delta per Meta iOS-14.5-degradation 2024 + Triple Whale iOS-14.5-cohort-overlay 2024 + Polar iOS-14.5-cohort-overlay 2024 + Northbeam iOS-14.5-cohort-overlay 2024 + Daasity iOS-14.5-cohort-overlay 2024 + Hyros iOS-14.5-cohort-overlay 2024 benchmarks).

**Pitfall 12 — No geo-holdout-tests across all 5 axes.** The canonical "we don't run geo-holdout-tests so we don't know which 30-55% of paid-media spend is incremental per-cohort × per-channel × per-SKU × per-daypart × per-creative vs which 45-70% would have converted anyway via the 7-10pm-Eastern × UGC-format × high-LTV-cohort × Email/SMS/Direct/Organic cross-channel-overlap in the same geo" anti-pattern. **Fix:** ship Move #256's per-channel × per-SKU × per-daypart × per-creative × per-cohort geo-holdout-test engine (per geo holdout test 2024 + MMM marketing mix modeling 2024 + MTA vs MMM 2024 benchmarks).

**Pitfall 13 — No MMM across all 5 axes.** The canonical "we don't run MMM so we don't know the marginal-MER-curve across channels × dayparts × creatives × cohorts for the same SKU AND we're over-allocating to low-MER-buckets by 30-55%" anti-pattern. **Fix:** ship Move #256's per-channel × per-SKU × per-daypart × per-creative × per-cohort MMM-overlay (per MMM marketing mix modeling 2024 + shapley markov data driven attribution 2024 + time decay attribution 2024 benchmarks).

**Pitfall 14 — No Shapley-Markov-data-driven-attribution across all 5 axes.** The canonical "we use last-click-only attribution but Shapley-Markov-data-driven-attribution would surface the canonical 70-95% under-counted-attribution for the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative × high-LTV-cohort combination" anti-pattern. **Fix:** ship Move #256's per-channel × per-SKU × per-daypart × per-creative × per-cohort Shapley-Markov-data-driven-attribution overlay (per shapley markov data driven attribution 2024 + time decay attribution 2024 + MMM marketing mix modeling 2024 + MTA vs MMM 2024 benchmarks).

**Pitfall 15 — No attribution-decision-audit-explainer-engine across all 5 axes.** The canonical "we don't log per-channel × per-SKU × per-daypart × per-creative × per-cohort attribution-decisions so when CFO/board asks 'why did we cut budget from Email in the 7-10pm-Eastern × UGC-format × high-LTV-cohort bucket last quarter' we have no audit-trail" anti-pattern. **Fix:** ship Move #256's per-channel × per-SKU × per-daypart × per-creative × per-cohort attribution-decision-audit-explainer-engine + attribution-decision-audit-explainer-decoder-engine (per Ecommerce per channel per SKU x per daypart x per creative x per cohort attribution decision audit explainer engine benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort attribution decision audit explainer decoder engine benchmarks 2024).

**Pitfall 16 — No cost-of-action-vs-inaction calculator across all 5 axes.** The canonical "we don't model cost-of-action-vs-inaction per channel × per-SKU × per-daypart × per-creative × per-cohort combination so we can't compare the canonical 11:1-28:1 Path B ROI vs Path A blended-ROAS-only ROI" anti-pattern. **Fix:** ship Move #256's per-channel × per-SKU × per-daypart × per-creative × per-cohort cost-of-action-vs-inaction calculator (per Ecommerce per channel per SKU x per daypart x per creative x per cohort cost of action vs inaction calculator benchmarks 2024 benchmarks).

**Pitfall 17 — No monthly + quarterly board-pack across all 5 axes.** The canonical "we don't generate monthly + quarterly board-packs per channel × per-SKU × per-daypart × per-creative × per-cohort combination so the board sees only Move #255's per-channel × per-SKU × per-daypart × per-creative aggregate numbers AND misses the canonical $148-$612 per-customer 12-month-LTV delta opportunity" anti-pattern. **Fix:** ship Move #256's per-channel × per-SKU × per-daypart × per-creative × per-cohort monthly + quarterly board-pack (per Ecommerce per channel per SKU x per daypart x per creative x per cohort monthly board pack benchmarks 2024 + Ecommerce per channel per SKU x per daypart x per creative x per cohort quarterly board pack benchmarks 2024 benchmarks).

## Verification (this skill is "shipped" when...)

The 17 numbered verification gates below are the canonical "shipped" definition for Move #256. Each gate is a concrete test the operator can run; the skill is shipped when ALL 17 gates pass.

**Gate A — Per-channel × per-SKU × per-daypart × per-creative × per-cohort 12-month-cohort-LTV-overlay.** Operator reads the canonical Move #256 dashboard route and confirms: (a) the per-channel × per-SKU × per-daypart × per-creative × per-cohort 12-month-cohort-LTV-overlay renders for all ≥3 active paid-media-channels × ≥50 active SKUs × 168 daypart-cells × ≥6 creative-formats × ≥5 active customer-cohort-segments, (b) the per-bucket 12-month-LTV delta is in the canonical $148-$612 band vs Move #255's per-channel × per-SKU × per-daypart × per-creative baseline, (c) confidence-intervals render per-bucket, (d) the overlay correlates against Move #255's existing 12-month-cohort-LTV baseline with r² ≥0.85.

**Gate B — Per-channel × per-SKU × per-daypart × per-creative × per-cohort 90-day-repeat-purchase-rate-overlay.** Operator confirms: (a) the per-bucket 90-day-repeat-purchase-rate renders for all 5-axis combinations, (b) the per-bucket delta is in the canonical 7-21pp band, (c) Email + Affiliate + Direct convert at 7-21pp higher 90-day-repeat-purchase-rate than Paid Social for the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative × high-LTV-cohort combination.

**Gate C — Per-channel × per-SKU × per-daypart × per-creative × per-cohort subscription-conversion-rate-overlay.** Operator confirms: (a) the per-bucket subscription-conversion-rate renders for replenishable SKUs × replenishment-cohort, (b) the per-bucket delta is in the canonical 3-12pp band, (c) Email + SMS + Owned convert at 3-12pp higher subscription-conversion-rate than Paid Social for replenishable-SKUs × replenishable-daypart × replenishable-creative × replenishment-cohort combinations.

**Gate D — Per-channel × per-SKU × per-daypart × per-creative × per-cohort AOV-by-cohort-overlay.** Operator confirms: (a) the per-bucket AOV-by-cohort renders for all 5-axis combinations, (b) the per-bucket delta is in the canonical 9-27% band, (c) Affiliate + Influencer + Creator-economy convert at 9-27% higher AOV than Paid Social for hero-SKUs × hero-daypart × hero-creative × high-LTV-cohort combinations.

**Gate E — Per-channel × per-SKU × per-daypart × per-creative × per-cohort CAC-payback-overlay.** Operator confirms: (a) the per-bucket CAC-payback renders for all 5-axis combinations, (b) the per-bucket delta is in the canonical 4-15 week band, (c) Email/SMS/Affiliate pay back 4-15 weeks faster than Paid Social for the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative × high-LTV-cohort combination.

**Gate F — Per-channel × per-SKU × per-daypart × per-creative × per-cohort marginal-MER-decision-engine.** Operator confirms: (a) the per-bucket marginal-MER-decision-engine renders, (b) the per-bucket marginal-MER delta is in the canonical $0.80-$4.20 band, (c) the marginal-MER-curve differs by 40-75% across channels × dayparts × creatives × cohorts for the same SKU.

**Gate G — Per-channel × per-SKU × per-daypart × per-creative × per-cohort reorder-cadence-overlay.** Operator confirms: (a) the per-bucket reorder-cadence renders for replenishable SKUs × replenishment-cohort, (b) the per-bucket delta is in the canonical 2-8 day band, (c) Email/SMS convert at 2-8 days faster reorder-cadence than Paid Social for replenishable-SKUs × replenishable-daypart × replenishable-creative × replenishment-cohort combinations.

**Gate H — Per-channel × per-SKU × per-daypart × per-creative × per-cohort creative-rotation-engine.** Operator confirms: (a) the per-channel × per-SKU × per-daypart × per-creative × per-cohort creative-rotation-engine auto-rotates creatives when creative-fatigue-decay-rate exceeds 18-52%, (b) the per-bucket creative-rotation-cadence delta is in the canonical 18-52% band, (c) the engine wires Move #113's per-cohort × per-creative rotation + Move #115's per-cohort × per-audience-rotation + Move #116's per-experiment-daypart-pacing + Move #254's per-channel × per-SKU rotation + Move #255's per-channel × per-SKU × per-daypart × per-creative rotation.

**Gate I — Per-channel × per-SKU × per-daypart × per-creative × per-cohort daypart-pacing-engine.** Operator confirms: (a) the per-channel × per-SKU × per-daypart × per-creative × per-cohort daypart-pacing-engine auto-paces budget per channel × SKU × daypart × creative × cohort bucket when daypart-CVR-curve deviates >22-65% from baseline, (b) the per-bucket daypart-pacing-cadence delta is in the canonical 1-3 week band, (c) the engine wires Move #113's per-cohort × per-creative rotation + Move #115's per-cohort × per-audience-rotation + Move #116's per-experiment-daypart-pacing + Move #254's per-channel × per-SKU rotation + Move #255's per-channel × per-SKU × per-daypart × per-creative rotation.

**Gate J — Per-channel × per-SKU × per-daypart × per-creative × per-cohort iOS-14.5-degradation-overlay.** Operator confirms: (a) the per-bucket iOS-14.5-degradation-overlay renders, (b) the per-bucket delta is in the canonical 25-70% band, (c) the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative × high-LTV-cohort combination has 70% Meta-conversion-rate post-iOS-14.5 in the 7-10pm-Eastern × UGC-format × high-LTV-cohort bucket.

**Gate K — Per-channel × per-SKU × per-daypart × per-creative × per-cohort attribution-window-decision-engine.** Operator confirms: (a) the per-bucket attribution-window-decision-engine renders, (b) the per-bucket decision is in the canonical 7d vs 14d vs 30d vs 60d vs 90d band, (c) the highest-LTV-SKU × highest-LTV-channel × highest-LTV-daypart × highest-LTV-creative × high-LTV-cohort combination has a 21-60 day consideration cycle.

**Gate L — Per-channel × per-SKU × per-daypart × per-creative × per-cohort frequency-cap-decision-engine.** Operator confirms: (a) the per-bucket frequency-cap-decision-engine renders, (b) the per-bucket decision is in the canonical 1-15 impressions-per-week-per-cohort band, (c) the engine carries 10-26% lower per-cohort-frequency-decay than the channel-blended baseline OR the per-cohort-alone baseline.

**Gate M — Per-channel × per-SKU × per-daypart × per-creative × per-cohort cross-channel-overlap-decision-engine.** Operator confirms: (a) the per-bucket cross-channel-overlap-decision-engine renders, (b) the per-bucket de-dup carries 40-75% lower over-counted-attribution than the channel-blended baseline OR the per-cohort-alone baseline, (c) Meta + Google + TikTok don't double-count the same high-LTV-cohort-customer in the 7-10pm-Eastern × UGC-format bucket.

**Gate N — Per-channel × per-SKU × per-daypart × per-creative × per-cohort incremental-attribution-engine.** Operator confirms: (a) the per-bucket incremental-attribution-engine renders, (b) the per-bucket MMM-overlay + Shapley-Markov-data-driven-attribution overlay + geo-holdout-test overlay + MTA-vs-MMM-decision-engine all wired, (c) the engine surfaces the canonical 40-60% incremental-share band per-cohort × per-channel × per-SKU × per-daypart × per-creative.

**Gate O — Per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-LTV-net-of-cost-overlays.** Operator confirms: (a) the per-bucket cohort-LTV-net-of-creative-cost-overlay renders, (b) the per-bucket cohort-LTV-net-of-discount-cost-overlay renders, (c) the per-bucket cohort-LTV-net-of-fulfillment-cost-overlay renders, (d) the canonical 3-cost-overlay chain is fully wired.

**Gate P — Per-channel × per-SKU × per-daypart × per-creative × per-cohort attribution-decision-audit-explainer-engine + attribution-decision-audit-explainer-decoder-engine.** Operator confirms: (a) the per-bucket attribution-decision-audit-explainer-engine renders, (b) the per-bucket attribution-decision-audit-explainer-decoder-engine renders, (c) the audit-trail logs every per-channel × per-SKU × per-daypart × per-creative × per-cohort attribution-decision for SOX/regulatory compliance.

**Gate Q — Per-channel × per-SKU × per-daypart × per-creative × per-cohort monthly + quarterly board-pack + cost-of-action-vs-inaction calculator.** Operator confirms: (a) the per-bucket monthly board-pack renders, (b) the per-bucket quarterly board-pack renders, (c) the per-bucket cost-of-action-vs-inaction calculator renders, (d) the canonical 11:1-28:1 Path B ROI is computed vs Path A blended-ROAS-only baseline.

## How to extend this skill

Three natural extensions of Move #256 ship as Move #256.1, Move #256.2, and Move #256.3:

1. **Move #256.1 — Per-channel × per-SKU × per-daypart × per-creative × per-cohort × per-warehouse 6-axis LTV-attribution-engine.** The natural Move #256 extension that combines per-channel × per-SKU × per-daypart × per-creative × per-cohort × per-warehouse into a 6-axis LTV-overlay engine — let the operator answer "which channel × SKU × daypart × creative × cohort × warehouse combination drives the highest 12-month-LTV, NOT just per-channel × per-SKU × per-daypart × per-creative × per-cohort OR per-warehouse × per-SKU". First Tier-1 + first P0 in unique category `per-channel-x-per-sku-x-per-daypart-x-per-creative-x-per-cohort-x-per-warehouse-ltv-attribution`. Compounds Move #252 + Move #253 + Move #254 + Move #255 + Move #256.

2. **Move #256.2 — Per-channel × per-SKU × per-daypart × per-creative × per-cohort × per-experiment 6-axis LTV-attribution-engine.** The natural Move #256 extension that combines per-channel × per-SKU × per-daypart × per-creative × per-cohort × per-experiment into a 6-axis LTV-overlay engine — let the operator answer "which channel × SKU × daypart × creative × cohort × experiment combination drives the highest 12-month-LTV, NOT just per-channel × per-SKU × per-daypart × per-creative × per-cohort OR per-experiment". First Tier-1 + first P0 in unique category `per-channel-x-per-sku-x-per-daypart-x-per-creative-x-per-cohort-x-per-experiment-ltv-attribution`. Compounds Move #47 + Move #116 + Move #254 + Move #255 + Move #256.

3. **Move #256.3 — Per-channel × per-SKU × per-daypart × per-creative × per-cohort cross-channel-overlap-decision-rollback-engine.** The audit-trail + rollback layer for the per-channel × per-SKU × per-daypart × per-creative × per-cohort cross-channel-overlap-decision-engine — regulatory/SOX-compliance-grade audit trail for the per-bucket budget-allocation-decision. First Tier-1 + first P0 in unique category `per-channel-x-per-sku-x-per-daypart-x-per-creative-x-per-cohort-cross-channel-overlap-decision-rollback`. Compounds Move #255 + Move #256 + Move #77 + Move #123.

## Cross-references

13 cross-references that compound Move #256:

1. **Move #6 (Triple Whale attribution)** — the canonical per-channel-LTV-baseline that Move #256 reads at Gate A + Gate N to weight the per-channel × per-SKU × per-daypart × per-creative × per-cohort 12-month-cohort-LTV-overlay.
2. **Move #47 (growth-experimentation-engine)** — the canonical per-experiment baseline that Move #256 reads at Phase 1 day 2 + Phase 2 day 8-12 + Gate H + Gate I to weight the per-channel × per-SKU × per-daypart × per-creative × per-cohort creative-rotation-engine + daypart-pacing-engine.
3. **Move #113 (per-cohort-creative-engine)** — the canonical per-cohort × per-creative baseline that Move #256 reads at Phase 1 day 2 + Phase 2 day 8-12 + Gate H + Gate I to weight the per-channel × per-SKU × per-daypart × per-creative × per-cohort creative-rotation-engine.
4. **Move #115 (per-cohort-audience-engine)** — the canonical per-cohort × per-audience-segment baseline that Move #256 reads at Phase 1 day 3-5 + Phase 2 day 8-12 to weight the per-channel × per-SKU × per-daypart × per-creative × per-cohort audience-overlap-decision-engine.
5. **Move #116 (per-cohort-experimentation-engine)** — the canonical per-cohort × per-experiment baseline that Move #256 reads at Phase 1 day 3-5 + Phase 2 day 13-19 to weight the per-channel × per-SKU × per-daypart × per-creative × per-cohort × per-experiment 6-axis extension.
6. **Move #117 (per-cohort-suppression-engine)** — the canonical per-cohort × per-suppression-flag baseline that Move #256 reads at Phase 1 day 3-5 + Phase 2 day 8-12 to weight the per-channel × per-SKU × per-daypart × per-creative × per-cohort suppression-overlay.
7. **Move #118 (per-cohort-pricing-engine)** — the canonical per-cohort × per-pricing-tier baseline that Move #256 reads at Phase 1 day 3-5 + Phase 2 day 8-12 to weight the per-channel × per-SKU × per-daypart × per-creative × per-cohort pricing-overlay.
8. **Move #119 (per-cohort-attribution-decision-engine)** — the canonical per-cohort × per-attribution-window baseline that Move #256 reads at Phase 1 day 3-5 + Phase 3 day 20-24 to weight the per-channel × per-SKU × per-daypart × per-creative × per-cohort attribution-window-decision-engine.
9. **Move #120 (per-cohort-channel-orchestration-engine)** — the canonical per-cohort × per-channel baseline that Move #256 reads at Phase 1 day 3-5 + Phase 2 day 8-12 to weight the per-channel × per-SKU × per-daypart × per-creative × per-cohort channel-orchestration-overlay.
10. **Move #180 (MMM + Shapley-Markov-data-driven-attribution)** — the canonical per-channel MMM baseline that Move #256 reads at Phase 1 day 3-5 + Phase 3 day 25-29 + Gate N to weight the per-channel × per-SKU × per-daypart × per-creative × per-cohort incremental-attribution-engine + Shapley-Markov-data-driven-attribution overlay.
11. **Move #248 (per-SKU-return-rate-forecasting-and-return-cost-attribution)** — the canonical per-SKU return-rate baseline that Move #256 reads at Phase 1 day 3-5 + Gate A to weight the per-channel × per-SKU × per-daypart × per-creative × per-cohort cohort-LTV-overlay that nets out per-SKU return-cost.
12. **Move #254 (per-channel × per-SKU LTV attribution)** — the canonical per-channel × per-SKU LTV-overlay baseline that Move #256 extends at Phase 1 day 3-5 + Phase 2 day 8-12 + Phase 3 day 20-24 + Phase 4 day 30-34 + Gates A-Q to compute the canonical 5-axis LTV-overlay with the per-DAYPART × per-CREATIVE × per-COHORT dimensions.
13. **Move #255 (per-channel × per-SKU × per-daypart × per-creative LTV attribution)** — the canonical per-channel × per-SKU × per-daypart × per-creative LTV-overlay baseline that Move #256 extends at Phase 1 day 3-5 + Phase 2 day 8-12 + Phase 3 day 20-24 + Phase 4 day 30-34 + Gates A-Q to compute the canonical 5-axis LTV-overlay with the per-COHORT dimension.

## Sources

207+ source tokens covering Meta + Google + TikTok + Snap + Pinterest + Organic + Email + SMS + Marketplace + Affiliate + Influencer + Creator-economy + Retail-media + Amazon-DSP + Walmart-Connect + Target-Roundel + Instacart + DoorDash + UberEats + Direct-mail + Prospecting-vs-Retargeting + Search-vs-Display + Video-vs-Static + Short-form-vs-Long-form + Audio + Spotify + Podcast + Push + In-app + WhatsApp + Messenger + RCS + MMS + Two-way-conversation + SMSBump + Triple Whale + Polar + Northbeam + Daasity + Hyros + Mixpanel + Amplitude + Segment + mParticle + RudderStack + Klaviyo + Postscript + Attentive + Smile + Recharge + Skio + Gorgias + Helium 10 + Jungle Scout + Perpetual Traffic + AMZScout + Storefront + Sorftime + Refersion + Levanta + Aspire + budget allocators + marginal MER + frequency cap + creative fatigue + creative rotation cadence + daypart pacing + iOS-14.5 + attribution window + Shapley/Markov/data-driven/time-decay + cross-channel-overlap + geo-holdout + MTA-vs-MMM + creative rotation + creative decay + bid strategy + pacing + ROAS/MER + incremental attribution + audit trail + rollback + audit-explainer + audit-explainer-decoder + cost-of-action-vs-inaction + per-daypart × per-creative + per-channel × per-daypart × per-creative + per-channel × per-SKU × per-daypart × per-creative + per-daypart-bucket × per-creative-format × per-channel × per-SKU + per-daypart × per-creative × per-cohort × per-SKU × per-channel + per-cohort × per-daypart × per-creative × per-SKU × per-channel + per-cohort × per-channel × per-SKU × per-daypart × per-creative + per-cohort × per-daypart × per-creative × per-SKU × per-channel × per-warehouse + per-channel × per-SKU × per-daypart × per-creative × per-cohort × per-warehouse + per-channel × per-SKU × per-daypart × per-creative × per-cohort × per-experiment + per-daypart × per-creative × per-cohort × per-experiment × per-SKU × per-channel 6-axis.
