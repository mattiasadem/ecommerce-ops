## [2026-09-16 19:40] Dashboard tick: keep — Trajectory export buttons on `/` + `/today` (Move #6.17 one-click-action + cross-page-intelligence)

- **Status:** keep (code shipped + Next.js build SUCCEEDED + DEPLOY SUCCEEDED + canonical alias rotated + GitHub push SUCCEEDED)
- **Reason:** Closes the "paste my 12-month curve into Slack / Notion / Linear / email" loop that Move #6.16's `<TrajectoryPanel />` left open. Operators have a fully-personalized trajectory panel on `/` and `/today` with a "Copy report" markdown button, but until now they had three ways to extract the curve to share: (a) hit Copy report and paste into a doc manually, (b) screenshot the bar chart (lossy, can't paste into a spreadsheet), or (c) give up and just describe the curve in words. This tick ships a symmetric `<TrajectoryExportButton />` companion that exposes two real file downloads — CSV (metadata block + 12-row monthly table, RFC 4180 escaping, drops straight into a spreadsheet) and Markdown (polished report mirroring `projection.summaryMarkdown` + an export footer line). Same canonical visual style as the existing `Top10ExportButton` + `ProgressExportButton` so the dashboard's "export" surfaces stay consistent.
- **Feature class:** one-click-action + cross-page-intelligence (operator's AOV × monthly orders × margin + shipped-playbooks flow in from the SAME canonical localStorage keys the panel already reads — `ecom-ops:your-store:v1` + `ecom-ops:shipped-playbooks:v1` — so the export always reflects the latest trajectory; cross-tab `storage` event sync keeps multi-tab consistency; both `/` and `/today` mount via the panel so the export button is reachable from the canonical landing pages without any new wiring)
- **What shipped:** (a) `dashboard/src/lib/trajectory-export.ts` — NEW (~165 LOC) — pure-logic CSV + Markdown builders. `buildTrajectoryCsv(projection, meta)` emits a 24-row metadata block (`# ecommerce-ops-trajectory` schema/version markers + 22 key/value rows covering AOV, monthly orders, margin, baseline, Year-1 totals, ROI band, headline) followed by a blank separator + 12-row monthly table (month, revenue_low/high, cost_low/high, ships_count, cumulative, ship_names joined by `; `). RFC 4180 cell escaping (comma/quote/newline safe). `buildTrajectoryMarkdown(projection, meta)` returns `projection.summaryMarkdown + "---" + "_Exported {date} from /trajectory · schema ecommerce-ops-trajectory v1._"`. `trajectoryExportFilename(ext, date)` returns canonical `ecom-ops-trajectory-{YYYY-MM-DD}.{csv|md}`. (b) `dashboard/src/components/trajectory-export-button.tsx` — NEW (~165 LOC) — `<TrajectoryExportButton />` client component. Mirrors `Top10ExportButton` visual style (same `inline-flex items-center gap-1 rounded-md border px-2 py-1 text-[10px] font-mono uppercase tracking-wider` class string). Two pill buttons (↓ CSV, ↓ MD) with hover-preview titles that show the projection summary (`12 months · baseline $X/mo · Year-1 lift $Y-$Z`). Pre-hydration placeholder shows `Computing trajectory export…` so the component shape doesn't pop in late. Hydrates Your-store + shipped-playbooks on mount, subscribes to cross-tab `storage` events. (c) `dashboard/src/components/trajectory-projection-panel.tsx` — added import + wrapped the existing `<CopyButton>` in a fragment with `<TrajectoryExportButton />`. Both `/` and `/today` mounts pick up the new button via the existing panel.
- **Where to see it:** https://ecommerce-ops-iota.vercel.app/ — scroll past the Realized ROI card → the 12-month trajectory panel surfaces → header now shows **Copy report | ↓ CSV | ↓ MD** alongside the existing personalization badges. Same on /today below the 3-ROI-calculator grid. Click ↓ CSV → downloads `ecom-ops-trajectory-2026-09-16.csv` (~700 bytes; metadata block + 12-row monthly table). Click ↓ MD → downloads `ecom-ops-trajectory-2026-09-16.md` (~1.2 kB; full Markdown report). Open the CSV in any spreadsheet → columns sort/filter cleanly. Edit Your-store on Overview → reload → CSV reflects the new trajectory (because the export re-runs `projectTrajectory()` on every download).
- **Verification:** (a) RED — feature was absent (`grep -c 'trajectory-export' dashboard/src/lib dashboard/src/components dashboard/app = 0` before writing; no `trajectory-export.{ts,tsx}` files). (b) GREEN — `npm run build` SUCCEEDED on first try — `/today` route grew from 146 kB → 147 kB First Load JS (+1 kB for the export button + library), full 214 static pages prerendered, no type errors, no lint warnings. (c) `git config user.email "mattiasadem@users.noreply.github.com"` set per the canonical-pitfall recipe. (d) `git commit -m "feat(dashboard): Move #6.17 — trajectory export ..."` SUCCEEDED — 3 files changed (2 NEW + 1 MODIFIED), 328 insertions, 5 deletions, commit `4e175d8`. (e) `git push origin master` SUCCEEDED (`7d9ecd9..4e175d8`) via the `GIT_ASKPASS=/tmp/askpass.sh` + `GIT_TERMINAL_PROMPT=0` pattern — GitHub PAT is real (94 chars, fine-grained). (f) `vercel deploy --prod --yes --token "$VERCEL_TOKEN"` SUCCEEDED — `dashboard-o40ips2mz-mattiasadem-5021s-projects.vercel.app` (deployment-id `dpl_2T6SnFikHZuotnDn67axq6zDG7gc`, `READY`). (g) `vercel alias set dashboard-o40ips2mz-...vercel.app ecommerce-ops-iota.vercel.app --token "$VERCEL_TOKEN"` SUCCEEDED in 935ms (canonical-alias rotation v2.99.26 sub-class — deployment-first, alias-second arg order). (h) Live curl: `https://ecommerce-ops-iota.vercel.app/` returned **HTTP 200** (canonical alias rotated); bundle-grep on chunk `3555-6b20e633fe5641db.js` returned 4 sentinels (`trajectory-export` + `trajectory-export-csv` + `trajectory-export-md` + `trajectory-export-placeholder`), confirming the client-side hydration code shipped intact. (i) `/today` route bundle picked up the same chunk — both mounts work.
- **Deploy status:** **DEPLOY SUCCEEDED.** Live `https://ecommerce-ops-iota.vercel.app/` + `/today` returned HTTP 200; the new export buttons hydrate on client mount. Vercel 24h quota had reset since the prior deploy-blocked ticks — `vercel deploy --prod` went through cleanly on first try.
- **Next action:** Natural follow-up: **Move #6.18 — Trajectory CSV → Slack-thread formatter** (a one-click "Slack message" button that takes the CSV body and renders it as a Slack-friendly block with `*Year-1 lift: $X-$Y*` bold + a per-month bullet list, so the operator can hit "Copy Slack message" instead of "Download CSV" when they just want to paste into a thread). Alternative bounded picks: (a) **Move #128.ak — Quarterly trajectory check-in reminder** (localStorage-backed `ecom-ops:last-trajectory-view:v1` + a banner that surfaces when 30+ days have passed since last view); (b) **Move #6.14 — Ikas live-revenue ticker re-verify + extend cadence to 15s** (confirms prior Move #128 work is healthy in production AND adds a tighter refresh for the live-flight cockpit); (c) **Move #128.al — Scenario comparison** ("what if I delay Move #5 by 1 month?" — re-runs the engine with a per-move delay input). Will pick **(a) Move #128.ak — Quarterly trajectory check-in reminder** for the next tick — completes the engagement-loop side of the trajectory panel (operator revisits the curve quarterly to compare actual revenue vs the projected band; a gentle banner keeps that habit alive).