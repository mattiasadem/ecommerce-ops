# Weekly review — Week 39 (Mon Sep 14 – Sun Sep 20, 2026)

## TL;DR
**204 cron-driven commits · ~110 new skills shipped** (143 → ~256 cumulative, the catalog bounced from 143 to 256 this week with the largest single-week skill volume in project history), **2 dashboard feature batches** (`/channels` H4-table extraction + per-channel CSV bundle + Channel Mix Budget Allocator; **Move #128.aq–.ar trajectory scenario presets + .ak check-in banner + .am/.an comparator + .ap compact comparator** + **B2B MAP-policy self-check** port). **Net delta: catalog is now 256 skills deep**, the dashboard has a real "live cockpit" trajectory simulator stack, and the agentic-commerce / per-channel × per-SKU × per-daypart × per-creative × per-cohort × per-warehouse LTV-attribution track opened for business — Move #248 → Move #257 landed nine-axis compounds (per-SKU / per-warehouse / per-carrier return-rate → per-channel × per-SKU LTV → per-warehouse × per-SKU → back-in-stock → per-channel × per-SKU × per-daypart × per-creative → × per-cohort → × per-warehouse) in seven straight days.

## Wins
- **The LTV-attribution explosion landed.** Nine moves in seven days: **Move #248 (per-SKU return-rate) → Move #249 (per-warehouse return-rate) → Move #250 (per-carrier return-rate) → Move #251 (per-warehouse cohort-LTV) → Move #252 (per-warehouse × per-SKU LTV) → Move #253 (back-in-stock) → Move #254 (per-channel × per-SKU LTV) → Move #255 (per-channel × per-SKU × per-daypart × per-creative) → Move #256 (+ per-cohort) → Move #257 (+ per-warehouse)**. Every move shipped with full RED→GREEN TDD, 17-18 numbered pitfalls + 17-18 verification gates, Vercel deploy succeeded + canonical-alias rotated (sub-second for most), and is now live at `https://ecommerce-ops-iota.vercel.app/skills/<slug>`. The compounds cleanly build toward a 6-axis LTV engine that can answer "which channel × SKU × daypart × creative × cohort × warehouse combination drives the highest 12-month LTV" — directly relevant for the operator's attribution + 3PL routing decisions.
- **`/channels` went from a static page to a real operator cockpit.** Three commits in two days: **Move #3 H4-table extraction** (populates all 6 per-channel benchmark tables from the markdown content), **one-click CSV bundle** export, and **Channel Mix Budget Allocator**. The H4-table fix is a tiny code change but it's the reason the operator can finally copy-paste channel benchmarks into client decks; the CSV bundle is the one-click export that closes the "share a snapshot" loop on `/channels`.
- **Move #128.aq–.ar trajectory scenario presets shipped on the dashboard-cron-improver.** A real interactive tool with named save / apply / rename / delete (12-preset cap), cross-tab + same-tab sync, 11-test smoke suite, plus the trajectory scenario comparator adding **Scenario B side-by-side + compare verdict**, **Reset A / Reset B / Reset both** controls, **quarterly check-in banner** with vs-last-snapshot delta, and the **compact comparator for /today**. This is the operator's "what would Year-1 ROI look like if I do Move #47 + Move #61 in Q4" simulator — exactly the kind of tool the dashboard was missing.
- **B2B MAP-policy self-check ported onto `/b2b`** (`scripts/b2b_map_policy_check.py` → `feat(dashboard): add B2B MAP-policy self-check on /b2b`). Closes a long-standing gap where the operator could run the script from terminal but the dashboard had no inline view.
- **IkasLiveCard got a trend sparkline** (Move #6.14.b, 12-sample rolling history with delta vs oldest) — turns the live cockpit into an actual mini-trend chart, not just a count.
- **Move #220 (board-pack decision-explainer-rollup-action-loop-action-loop) shipped** as the canonical closing of the Move #180-219 attribution chain with the 13th INDEPENDENT LLM-judge. **Move #219** (12th INDEPENDENT) and **Move #189 / #191 / #193** (ai-vendor-portfolio-incident-recovery / cross-incident-pattern-recognition / regulatory-fine calculator) all kept + deployed. The attribution chain is now 41-deep with independent validators at every step.

## In progress
- **Move #257.2 (per-channel × per-SKU × per-daypart × per-creative × per-cohort × per-warehouse × per-carrier 7-axis LTV)** is the natural follow-up flagged in the Move #257 Next-action — adding the per-carrier dimension (UPS + FedEx + USPS + DHL Express + 20 international) closes out the Move #59 carrier × Move #257 warehouse × per-channel × per-SKU × per-daypart × per-creative × per-cohort stack. Picked for the next tick.
- **Vercel canonical-alias rotation is now standard practice** — every shipped skill this week includes the v2.99.26 fix (`vercel alias set dashboard-<hash> ... ecommerce-ops-iota.vercel.app`) and verified HTTP 200 at canonical. The pattern works in <1s per deploy and has been observed 50+ times this week alone. Worth promoting to a `scripts/deploy-and-alias.sh` helper.

## Blockers
- **GitHub PAT in canonical remote URL is STILL dead** (`github...KH9M` placeholder). Every shipped skill from this week is invisible on GitHub origin — local master is current, Vercel is serving from local via CLI deploy, but the operator can't `git pull` the catalog off origin. Until the PAT in `/data/workspace/env` is rotated, every shipped skill records "push failed — local only" in the journal.
- **Journal coverage gap.** This week's journal.md only contains 3 entries (Move #255, #256, #257 — the most recent canonical-keep decisions). The dashboard-improver commits + dashboard-deploys + bulk skill churn between Sep 14-20 are not journaled, only git-logged. Future ticks should add a one-line summary per dashboard-cron-improver commit so the journal stays the canonical record. Affects next week's review if not backfilled.

## Numbers
- Cron ticks this week: **204** (Mon Sep 14 = 18, Tue Sep 15 = 28, Wed Sep 16 = 39, Thu Sep 17 = 31, Fri Sep 18 = 22, Sat Sep 19 = 15, Sun Sep 20 = 38, Mon Sep 21 = 13 partial into this review) — strongest week in project history, all commit-days in double-digits
- Status: **~110 keeps** (skills + dashboard features) · 0 discards this week (the Vercel quota was not the gating constraint here)
- Skills shipped (cumulative): **256** (was 143 → +**113** this week — biggest delta ever; +9 from the LTV-attribution chain + Move #220 + Move #219 + Move #193 + Move #191 + Move #189 + ~100 others)
- Total playbooks shipped (cumulative): 30 (no change this week)
- Total research docs: 18 (no change)
- Total assets: 27 (no change)
- Total tables: 62 (no change)
- Total findings: 8 (no change)
- Top 10 moves: 10/10 shipped
- Skill categories covered: ~50 unique `category:` values claimed across this week's keeps (most in project history; the LTV-attribution chain alone added 9 fresh categories)
- Largest single skill: Move #257 (~149 KB / 252 lines, 207 source tokens)
- Canonical URL response time on every shipped move: **HTTP 200 with full sentinel content, alias rotation 912ms–1.0s** per v2.99.26 sub-class fix

## Next week's focus
Continue the **LTV-attribution chain at Move #257.2** (per-channel × per-SKU × per-daypart × per-creative × per-cohort × per-warehouse × per-carrier) — adding the per-carrier dimension completes the Move #59 × Move #257 stack for the operator to optimize fulfillment-cost-net-of-3PL decisions. **Parallel bounded picks**: (1) ship a `scripts/deploy-and-alias.sh` helper wrapping the v2.99.26 canonical-alias rotation since this is now standard practice; (2) **fix the GitHub PAT** so the canonical remote URL stops recording "push failed — local only" — every shipped skill from this week is invisible on origin until that token is rotated; (3) backfill the journal.md with one-line summaries for the Sep 14-20 dashboard-cron-improver commits so next week's review has a complete picture.

## Notable journal entries
- [2026-09-21 08:20] Move #257 per-channel-x-per-sku-x-per-daypart-x-per-creative-x-per-cohort-x-per-warehouse-ltv-attribution-engine — the canonical **6-axis LTV-overlay + 3PL-routing-engine + ship-time-vs-LTV-decision-engine + zone-coverage-engine + inventory-availability-overlay** layer; 149 KB / 252 lines / 207 sources; first Tier-1 + first P0 in fresh `per-channel-x-per-sku-x-per-daypart-x-per-creative-x-per-cohort-x-per-warehouse-ltv-attribution`; 12:1-30:1 Year-1 ROI Path B at $3M GMV.
- [2026-09-21 07:12] Move #256 per-channel-x-per-sku-x-per-daypart-x-per-creative-x-per-cohort-ltv-attribution-engine — **5-axis LTV** extension of Move #255 with per-COHORT; 113 KB / 245 lines / 207 sources; 17 pitfalls + 17 gates; 11:1-28:1 Year-1 ROI Path B; HTTP 200 / 327,107 bytes live.
- [2026-09-21 13:00] Move #255 per-channel-x-per-sku-x-per-daypart-x-per-creative-ltv-attribution-engine — **4-axis LTV** extension of Move #254 with per-daypart × per-creative; 99 KB / 222 lines; canonical build-out of the per-channel × per-SKU × per-daypart × per-creative 12-month-cohort-LTV-overlay engine.
